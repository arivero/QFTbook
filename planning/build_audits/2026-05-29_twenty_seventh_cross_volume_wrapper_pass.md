# 2026-05-29 Twenty-Seventh Cross-Volume Wrapper Pass

## Scope

Continued the active proof-form audit for issue #691 after checkpoint
`b86bde80`.  The pass targeted short or cue-heavy theorem-family statements
whose proof content was local algebra, scale bookkeeping, or a consequence of
a strong upstream hypothesis rather than theorem-level machinery.

## Demotions

Demoted nine theorem-family wrappers to prose calculations or construction
paragraphs while preserving their formulas and assumptions:

- one-loop SPDE relative-scale gap for a local subtraction;
- rectangular area coefficient versus static-potential slope;
- self-dual gauging producing a noninvertible defect;
- conformal multiplet generated by a primary datum;
- WZW level quantization;
- separated-point scaling from source-extended RG;
- linearized RG eigenvalues and operator dimensions;
- Liouville improved central charge and exponential weights;
- finite linearized collision operator.

The common reason is that the useful content is the displayed calculation or
the precise upstream hypothesis.  Presenting these as propositions created too
much proof-form weight for bookkeeping steps.

## Retained After Reading

This pass also read several short high-signal candidates that remain
theorem-family statements because they provide reusable mechanisms rather than
mere substitutions: mass gap from Euclidean clustering, finite Grassmann
reflection positivity, projective dual-norm kernel bounds, holomorphic
semigroup reconstruction of a positive generator, BV pushforward preservation
of the QME, Hadamard-function change isomorphism, transfer-matrix
commutativity, Rayleigh--Ritz convergence, finite-volume pole reconstruction,
Haag--Ruelle isometry, and finite BF linking phase.

## Guardrails

`tools/audit_theorem_form.py` now rejects recurrence of the nine demoted
titles as theorem/proposition/lemma/corollary headings.

## Current Counts

After the demotions:

- theorem/proposition/lemma/corollary environments: 560
- proof environments: 555
- strict high-signal short/cue-heavy queue: 25 candidates.

The queue remains a manual-reading queue, not a defect count.

## Verification

- stale-label scan for the nine removed labels: clean;
- `python3 tools/audit_theorem_form.py`: clean;
- `tools/audit_negative_scope_prose.py`: clean;
- `tools/audit_monograph_text.sh`: clean;
- `python3 tools/audit_unnumbered_display_labels.py`: clean;
- `tools/audit_chapter_dossiers.sh`: clean after adding the missing metadata
  headings to the Volume IV Chapter 04 dossier;
- `git diff --check`: clean;
- `tools/build_monograph.sh`: clean;
- `pdfinfo monograph/tex/main.pdf | rg '^Pages:'`: 2579 pages.
