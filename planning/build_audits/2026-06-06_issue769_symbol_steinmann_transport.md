# 2026-06-06 Issue 769 Symbol Steinmann Transport Audit

## Scope

- Added `ca:symbol-steinmann-transport-check` to Volume II Chapter 6 between
  the two-letter master-transport audit and the production master lane.
- Added a companion exact-rational check in
  `calculation-checks/generalized_unitarity_reduction_checks.py`.
- Updated the Chapter 6 dossier and calculation-check inventory.

## Depth Audit

- This pass is architectural, not a standalone lemma annexation.  It connects
  canonical-form master equations, ordered symbol words, physical sequential
  discontinuities, and the later Steinmann theorem in one reader-facing
  mechanism.
- The finite model preserves nonzero overlapping single cuts while forcing
  both overlapping ordered double words to vanish after the physical
  projection, and it separately allows a compatible sequential word to survive.
- The model is explicitly framed as a residue/projection test, not as a
  claimed global flat connection for a concrete integral family.
- The negative control preserves the single cuts but violates both overlapping
  ordered-word constraints.  This addresses the specific risk that generalized
  unitarity or symbol data could be overread as a physical amplitude statement
  before causal channel compatibility, sheet data, boundary constants, and
  lower-sector completion are fixed.

## Scope Guard

- The new monograph prose does not add planning directives, issue metadata, or
  review instructions to the TeX.
- The content stays on loop-amplitude physics: physical discontinuities,
  causal Steinmann constraints, branch data, master projections, and observable
  assembly.  It avoids a detached symbolic-combinatorics detour.
