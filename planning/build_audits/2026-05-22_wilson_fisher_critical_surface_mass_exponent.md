# Wilson-Fisher Critical Surface And Mass Exponent Pass

Date: 2026-05-22

Scope:
- `monograph/tex/volumes/volume_ii/chapter14_the_wilson_fisher_fixed_point_and_scaling_operators.tex`
- `planning/chapter_dossiers/volume_ii/chapter15_wilson_fisher_fixed_point_scaling_operators.md`

Changes:
- Scoped the Wilson-Fisher quartic fixed point to the massless critical
  surface rather than to arbitrary massive trajectories.
- Stated the `Z_2`-even RG chart assumption and recorded the odd source
  `h[\phi]` as a relevant direction when the symmetry is not imposed.
- Added the mass/temperature perturbation generated by `[O_2]`, derived
  `y_t = D - Delta_{phi^2} = 2 - epsilon/3 + O(epsilon^2)`, and derived
  `nu = 1/y_t = 1/2 + epsilon/12 + O(epsilon^2)`.
- Added and visually corrected a critical-surface figure showing the
  `Z_2`-even slice, the relevant mass direction, and the trajectory into the
  Wilson-Fisher fixed point.
- Updated the chapter dossier so future passes preserve the `Z_2`-even
  qualification and the distinction between the bare coordinate and the finite
  renormalized 1PI coordinate.

Verification:
- `git diff --check`
- `tools/audit_monograph_text.sh`
- `tools/build_monograph.sh`
- Rendered and inspected PDF pages around Chapter 36, including Figure 36.4,
  after fixing an initial label collision between `critical surface` and
  `Gaussian`.
