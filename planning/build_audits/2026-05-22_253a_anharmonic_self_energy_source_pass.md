# 2026-05-22 253a Anharmonic Oscillator and Self-Energy Source Pass

## Scope

- Source block: handwritten 253a pp. 30--43.
- Manuscript home:
  `monograph/tex/volumes/volume_i/chapter05_correlation_functions_wick_rotation_and_gaussian_integrals.tex`.
- Dossier:
  `planning/chapter_dossiers/volume_i/chapter05_euclidean_correlators_gaussian_perturbation.md`.
- Coverage register:
  `planning/source_inventory/253a_253b_no_skip_coverage_register.md`.

## Source Checks

- Checked the anharmonic oscillator definitions
  \(\widehat H=\widehat p^2/2+\widehat q^2/2+g\widehat q^4/4!\) and
  \(L_E=(q')^2/2+q^2/2+gq^4/4!\).
- Restored the source warning that the perturbative expansion is a
  coefficientwise regulated expansion, not an assumed convergent
  infinite-dimensional integral.
- Checked the first-order vacuum diagram
  \(-g\int\dd\tau\,G_0(\tau,\tau)^2/8\), including its proportionality to the
  Euclidean time interval.
- Restored the order-\(g^2\) vacuum-graph row with disconnected diagrams and
  the two connected source topologies: two lines plus tadpoles and four lines
  between the two vertices.
- Restored the full linked-cluster counting:
  \(\sum_\ell m_\ell=K\), \(\sum_\ell \ell m_\ell=n\),
  \(n!/\prod_\ell(\ell!)^{m_\ell}m_\ell!\), and the product-to-exponential
  derivation of \(Z_g/Z_0\).
- Added an explicit counted three-vertex connected vacuum topology with
  \(N_\triangle=\binom{4}{2}^3(2!)^3=1728\) and coefficient \(-g^3/48\).
- Checked the transition-kernel interpretation
  \(Z_g(T;q_f,q_i)=\langle q_f|e^{-T\widehat H}|q_i\rangle\) and the
  ground-state-energy extraction from \(\log Z_g\).
- Checked the normalized two-point function, vacuum-bubble cancellation, and
  Euclidean ordering for \(\tau>0\) and \(\tau<0\).
- Restored the order-\(g\) tadpole Fourier calculation, including the loop
  integral \(G_0(0)=\int\dd k\,\widetilde G_0(k)/(2\pi)=1/2\).
- Checked the source row of second-order connected two-point diagrams and the
  separation between the self-energy insertions and the chain generated by the
  geometric series.
- Checked the amputated self-energy convention,
  \[
    \Sigma(k)
    =
    -\frac{g}{4}
    +
    \frac{g^2}{8}
    \left(\frac14+\frac{1}{k^2+9}\right)
    +O(g^3),
  \]
  the corresponding \(\widetilde G(k)\), and the pole extraction
  \(E_1-E_0=1+g/8-g^2/32+O(g^3)\).

## Render Checks

- Built `monograph/tex/main.pdf` with `tools/build_monograph.sh`.
- Rendered physical PDF pages 72--78 to
  `/tmp/qft_ch8_anharmonic-072.png` through
  `/tmp/qft_ch8_anharmonic-078.png`.
- Inspected the order-\(g^2\) vacuum-graph row, linked-cluster formulas,
  counted triangle graph, vacuum-bubble cancellation figure, connected
  two-point graph row, second-order two-point graph row, self-energy chain, and
  complex \(k\)-plane pole contour.  The first build caught an overwide
  triangle formula; it was split into a centered diagram and an aligned
  equation.  The final build log is clean.

## Verification Commands

- `tools/build_monograph.sh`
- `tools/audit_monograph_text.sh`
- `git diff --check`

## Result

The 253a pp. 30--43 anharmonic oscillator, vacuum diagrams, two-point
function, self-energy, and spectral extraction block is marked certified in
the source coverage register.
