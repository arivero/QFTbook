\chapter{Vertex Operator Algebras, Modular Sewing, and Logarithmic CFT}
\label{ch:voa-modular-sewing-logarithmic-cft}

\ConventionsEuclidean

\section{Chiral Algebra Data}

Two-dimensional conformal field theory has an additional algebraic structure:
local holomorphic fields may be multiplied by operator products that depend
only on one complex coordinate.  A vertex operator algebra is the precise
algebraic encoding of this chiral operator product.

\begin{definition}[Vertex operator algebra]
\label{def:vertex-operator-algebra}
A vertex operator algebra consists of the following data.
\begin{enumerate}
\item A complex vector space
\[
  V=\bigoplus_{n\in\mathbb Z}V_n
\]
with finite-dimensional graded pieces and \(V_n=0\) for \(n\) sufficiently
negative.
\item A vacuum vector \(\mathbf 1\in V_0\).
\item A state-field map
\[
  Y(\,\cdot\,,z):V\longrightarrow \operatorname{End}(V)[[z,z^{-1}]],
  \qquad
  a\longmapsto Y(a,z)=\sum_{m\in\mathbb Z}a_m z^{-m-1}.
\]
\item A conformal vector \(\omega\in V_2\), whose modes
\[
  Y(\omega,z)=\sum_{n\in\mathbb Z}L_n z^{-n-2}
\]
obey the Virasoro algebra
\[
  [L_m,L_n]=(m-n)L_{m+n}
  +\frac{c}{12}(m^3-m)\delta_{m+n,0}.
\]
\end{enumerate}
These data satisfy:
\[
  Y(\mathbf 1,z)=\operatorname{id}_V,\qquad
  Y(a,z)\mathbf 1\in a+zV[[z]],
\]
\[
  [L_{-1},Y(a,z)]=\partial_zY(a,z),
  \qquad
  L_0a=na\quad\text{for }a\in V_n,
\]
and locality:
for every \(a,b\in V\), there is \(N\ge0\) such that
\[
  (z-w)^N [Y(a,z),Y(b,w)]=0
\]
as a formal distribution on \(V\).
\end{definition}

The modes \(a_m\) are operators on \(V\).  If \(a\) has conformal weight
\(\Delta_a\), then \(a_m\) has degree \(\Delta_a-m-1\).  The vacuum axiom says
that \(a\) is recovered from the field \(Y(a,z)\) by acting on the vacuum and
then taking the value at \(z=0\):
\[
  a=\left. Y(a,z)\mathbf 1\right|_{z=0}.
\]
The locality axiom is the algebraic form of the chiral OPE.  It implies the
Jacobi identity for modes, and conversely the Jacobi identity packages
locality, associativity, and commutativity of OPE expansions.

\begin{proposition}[OPE from VOA locality]
\label{prop:voa-ope-from-locality}
For \(a,b\in V\), the product \(Y(a,z)Y(b,w)\) has an expansion in the region
\(|z|>|w|\) of the form
\[
  Y(a,z)Y(b,w)
  =
  \sum_{m\in\mathbb Z}Y(a_m b,w)(z-w)^{-m-1},
\]
where for each matrix element only finitely many positive powers of
\((z-w)^{-1}\) occur.
\end{proposition}

\begin{proof}
By lower truncation, \(a_m b=0\) for all sufficiently large \(m\).  Locality
gives \(N\) such that
\[
  (z-w)^N Y(a,z)Y(b,w)=(z-w)^N Y(b,w)Y(a,z).
\]
Taking the formal residue
\[
  a_m b=\operatorname{Res}_{z-w}(z-w)^mY(a,z)b
\]
defines the coefficient of \((z-w)^{-m-1}\).  Multiplying the proposed OPE by
\((z-w)^N\), both sides become the same formal Laurent series because the
residues of all principal parts agree and the remaining part is regular at
\(z=w\).  The truncation of \(a_m b\) gives the stated finiteness.
\end{proof}

\section{Modules, Characters, and Chiral Blocks}

A \(V\)-module \(M\) is a graded vector space with fields
\[
  Y_M(a,z)=\sum_{m\in\mathbb Z}a^M_m z^{-m-1}
\]
obeying the same vacuum, translation, and locality identities with one entry
acting on \(M\).  The conformal vector gives an operator \(L_0\) on \(M\).
For an ordinary positive-energy module,
\[
  M=\bigoplus_{\lambda\in\mathbb C}M_\lambda,\qquad
  \dim M_\lambda<\infty,
\]
and the set of \(\lambda\)'s is bounded below in every coset modulo
\(\mathbb Z\).  The character is
\begin{equation}
  \chi_M(\tau)
  =
  \operatorname{Tr}_M q^{L_0-c/24},
  \qquad
  q=\ee^{2\pi\ii\tau},
  \qquad
  \operatorname{Im}\tau>0 .
  \label{eq:voa-character}
\end{equation}
The shift by \(c/24\) is the cylinder vacuum energy fixed by the conformal map
from the plane to the circle.

Let \(C\) be a compact Riemann surface with marked points \(p_i\), local
coordinates \(z_i\) at the marked points, and \(V\)-modules \(M_i\).  A chiral
conformal block is a linear functional
\[
  \mathcal F:\bigotimes_i M_i\longrightarrow\mathbb C
\]
that obeys the Ward identities for meromorphic chiral fields on \(C\).  For
the stress tensor, the identity says that inserting a meromorphic vector field
regular away from the \(p_i\)'s acts by the sum of the corresponding Virasoro
modes at the marked points and gives zero.  For a general chiral state
\(a\in V\), the meromorphic one-form valued in the field \(a\) gives the same
condition with modes of \(Y_{M_i}(a,z_i)\).

On the sphere with three marked points, conformal blocks are intertwiners.
If \(M_i,M_j,M_k\) are simple modules, the fusion coefficient is
\[
  N_{ij}{}^k
  :=
  \dim \operatorname{CB}_{0,3}(M_i,M_j,M_k^\vee),
\]
where \(M_k^\vee\) is the contragredient module.  This definition is intrinsic:
it counts chiral three-point structures, not a particular coordinate
presentation of a three-point function.

\section{Zhu Algebra and Top Levels}

The character and sewing constructions use the whole graded module.  The
classification of ordinary modules begins with a smaller algebra obtained
from genus-one coordinate changes.  This algebra is Zhu's algebra.  Its role
in the monograph is structural: it converts the lowest-energy part of a
positive-energy module into a finite algebraic object, while leaving the
reconstruction of the full module to the VOA theorem boundary.

Let \(a\in V\) be homogeneous of conformal weight \(\Delta_a\), and write
\[
  Y(a,z)=\sum_{n\in\mathbb Z}a_n z^{-n-1}.
\]
Define bilinear operations on \(V\) by
\begin{align}
  a*b
  &:=
  \operatorname{Res}_{z=0}
  \frac{(1+z)^{\Delta_a}}{z}Y(a,z)b
  =
  \sum_{j\ge0}\binom{\Delta_a}{j}a_{j-1}b,
  \label{eq:zhu-star-product}\\
  a\circ b
  &:=
  \operatorname{Res}_{z=0}
  \frac{(1+z)^{\Delta_a}}{z^2}Y(a,z)b
  =
  \sum_{j\ge0}\binom{\Delta_a}{j}a_{j-2}b .
  \label{eq:zhu-circle-product}
\end{align}
The sums are finite on each pair \(a,b\) by lower truncation.  Let
\[
  O(V)=\operatorname{span}_{\mathbb C}\{a\circ b\mid a,b\in V
  \text{ homogeneous}\}.
\]

\begin{definition}[Zhu algebra]
\label{def:zhu-algebra}
The Zhu algebra of a VOA \(V\) is the quotient vector space
\begin{equation}
  A(V)=V/O(V)
  \label{eq:zhu-algebra-quotient}
\end{equation}
with product
\begin{equation}
  [a]\,[b]:=[a*b].
  \label{eq:zhu-product-on-classes}
\end{equation}
The formal identities below imply that this product is independent of the
chosen representatives and is associative; they are the same component-Jacobi
residue identities used in Proposition~\ref{prop:zhu-top-level-action}.  We
denote the class of \(a\in V\) by \([a]\in A(V)\).
\end{definition}

The exponent \((1+z)^{\Delta_a}\) is not decorative.  It is the coordinate
factor relating the punctured plane coordinate to the genus-one coordinate
used in torus one-point functions.  The quotient by \(O(V)\) removes the
descendant terms that vanish on lowest-energy states.

\begin{proposition}[Top-level action]
\label{prop:zhu-top-level-action}
Let \(M\) be an ordinary positive-energy \(V\)-module with lowest conformal
weight \(h_M\), so that
\[
  M=\bigoplus_{n\ge0}M_{h_M+n}.
\]
For a homogeneous \(a\in V\), set
\begin{equation}
  o_M(a):=a_{\Delta_a-1}\big|_{M_{h_M}} .
  \label{eq:zhu-zero-mode-action}
\end{equation}
Then \(o_M(a\circ b)=0\) on \(M_{h_M}\), and
\begin{equation}
  o_M(a*b)=o_M(a)o_M(b).
  \label{eq:zhu-top-product}
\end{equation}
Thus \(M_{h_M}\) is an \(A(V)\)-module.
\end{proposition}

\begin{proof}
The mode \(a_n\) sends an \(L_0\)-eigenvector of weight \(\lambda\) to weight
\begin{equation}
  \lambda+\Delta_a-n-1.
  \label{eq:voa-mode-weight-shift}
\end{equation}
Thus every lowest-weight vector \(v\in M_{h_M}\) is killed by \(u_n\) whenever
\(u\) is homogeneous and \(n>\Delta_u-1\).  Equivalently,
\begin{equation}
  M_{h_M}\subset
  \Omega(M):=\{v\in M\mid u_n v=0
  \text{ for all homogeneous }u\in V,\ n>\Delta_u-1\}.
  \label{eq:zhu-omega-subspace}
\end{equation}
The remaining step is a finite component calculation from the module Jacobi
identity.  In mode form, that identity says
\begin{align}
  \sum_{i\ge0}\binom{m}{i}(a_{n+i}b)_{m+k-i}
  &=
  \sum_{i\ge0}(-1)^i\binom{n}{i}
  \bigl(a_{m+n-i}b_{k+i}
  -(-1)^n b_{n+k-i}a_{m+i}\bigr).
  \label{eq:borcherds-component-identity}
\end{align}
All sums are finite on a fixed vector by lower truncation.  Substitute
\(n=j-2\) and sum with the coefficients
\(\binom{\Delta_a}{j}\) from \eqref{eq:zhu-circle-product}.  The resulting
finite sum is reorganized by the binomial identity
\((1+z)^{\Delta_a}=(1+z)(1+z)^{\Delta_a-1}\); every summand in
\(o_M(a\circ b)v\) contains a factor \(u_rv\) with \(r>\Delta_u-1\), hence
vanishes for \(v\in M_{h_M}\).  Substituting \(n=j-1\) and summing with the
coefficients in \eqref{eq:zhu-star-product} leaves exactly the term
\(a_{\Delta_a-1}b_{\Delta_b-1}v\); the remaining terms again contain a mode
killing \(\Omega(M)\).  Therefore
\begin{equation}
  o_M(a\circ b)v=0,\qquad
  o_M(a*b)v=o_M(a)o_M(b)v
  \label{eq:zhu-top-level-identities}
\end{equation}
for every \(v\in M_{h_M}\).  This proves that \(O(V)\) is in the kernel of
the top-level action and that the induced action is multiplicative.
\end{proof}

\begin{quotedtheorem}[Zhu classification theorem]
\label{qthm:zhu-classification}
Let \(V\) be a grading-restricted VOA of CFT type:
\[
  V_n=0\quad(n<0),\qquad V_0=\mathbb C\mathbf 1,\qquad
  \dim V_n<\infty .
\]
For every simple ordinary positive-energy \(V\)-module \(M\), the lowest
space \(M_{h_M}\) is a simple \(A(V)\)-module.  Conversely, every simple
\(A(V)\)-module \(U\) has a universal \(N\)-gradable weak \(V\)-module
generated from \(U\) as its top level; when the irreducible quotient is
ordinary, this construction is inverse to \(M\mapsto M_{h_M}\).  In
particular, if every simple \(A(V)\)-module induces an ordinary simple module,
then simple ordinary positive-energy \(V\)-modules are classified by simple
\(A(V)\)-modules.

If \(V\) is rational in the sense that the ordinary module category is
semisimple with finitely many simple objects and finite-dimensional graded
pieces, then \(A(V)\) is finite-dimensional and semisimple.  If, in addition,
\[
  C_2(V):=\operatorname{span}_{\mathbb C}\{a_{-2}b\mid a,b\in V\}
  \quad\text{has finite codimension in }V,
\]
then the same finite label set appears in the vector of characters
\((\chi_i(\tau))_{i\in I}\) entering the modular-invariance theorem.
\end{quotedtheorem}

\begin{example}[Ising Zhu algebra as a top-weight test]
\label{ex:ising-zhu-algebra-top-weights}
For the Ising VOA \(L(1/2,0)\), the Zhu algebra is generated by the class
\([\omega]\) of the conformal vector.  The three simple modules have top
weights
\[
  0,\qquad \frac1{16},\qquad \frac12 .
\]
Equivalently, the semisimple Zhu algebra is the quotient
\begin{equation}
  A(L(1/2,0))
  \cong
  \mathbb C[x]\Big/
  \left(x\left(x-\frac1{16}\right)\left(x-\frac12\right)\right),
  \label{eq:ising-zhu-polynomial}
\end{equation}
where \(x\) acts as \(L_0\) on the top level.  The idempotents of this
three-dimensional algebra are the Lagrange polynomials
\begin{align}
  e_0
  &=
  32\left(x-\frac1{16}\right)\left(x-\frac12\right),
  \\
  e_{1/16}
  &=
  -\frac{256}{7}x\left(x-\frac12\right),
  \\
  e_{1/2}
  &=
  \frac{32}{7}x\left(x-\frac1{16}\right).
  \label{eq:ising-zhu-idempotents}
\end{align}
They obey \(e_h(h')=\delta_{h,h'}\), \(e_h^2=e_h\) in the quotient
\eqref{eq:ising-zhu-polynomial}, and \(e_0+e_{1/16}+e_{1/2}=1\).  Thus they
project to the vacuum, spin, and energy top levels, respectively.
\end{example}

\section{Unitary Virasoro Minimal Models}
\label{sec:unitary-virasoro-minimal-models}

The preceding VOA definitions are deliberately general.  The smallest
nontrivial unitary chiral CFTs arise when the chiral algebra is just the
Virasoro algebra and the representation category is finite.  This finiteness
is not automatic: it is a consequence of null vectors in highest-weight
Virasoro modules together with positivity.

Let \(M(c,h)\) be the Verma module generated by a highest-weight vector
\(\ket h\) satisfying
\[
  L_0\ket h=h\ket h,\qquad L_n\ket h=0\quad(n>0),
\]
for the Virasoro algebra of central charge \(c\).  In radial quantization of
a unitary chiral theory,
\[
  L_n^\dagger=L_{-n}.
\]
Thus each finite descendant level carries a Hermitian Gram form.  The first
two levels already show the mechanism.  At level one,
\[
  \|L_{-1}\ket h\|^2
  =
  \langle h|L_1L_{-1}|h\rangle
  =
  2h,
\]
so a unitary module has \(h\ge0\), and \(h=0\) makes \(L_{-1}\ket0\) a null
translation descendant of the vacuum.  At level two, in the ordered basis
\((L_{-2}\ket h,L_{-1}^2\ket h)\), the Gram matrix is
\begin{equation}
  M^{(2)}(c,h)
  =
  \begin{pmatrix}
    4h+\frac c2 & 6h\\
    6h & 4h(2h+1)
  \end{pmatrix}.
  \label{eq:virasoro-level-two-gram}
\end{equation}
Indeed,
\[
  \langle h|L_2L_{-2}|h\rangle
  =
  \langle h|(4L_0+c/2)|h\rangle,
\]
\[
  \langle h|L_2L_{-1}^2|h\rangle
  =
  6h,
\]
and
\[
  \langle h|L_1^2L_{-1}^2|h\rangle
  =
  4h(2h+1),
\]
obtained by commuting positive modes to the right until they annihilate
\(\ket h\).  If \(\det M^{(2)}(c,h)=0\) and \(h\ne0,-1/2\), the null vector is
\begin{equation}
  \left(
    L_{-2}-\frac{3}{2(2h+1)}L_{-1}^2
  \right)\ket h.
  \label{eq:level-two-virasoro-null-vector}
\end{equation}
Null descendants are not optional in a unitary CFT: they have zero inner
product with every state and must be quotiented to obtain the physical
irreducible module.

\begin{quotedtheorem}[Unitary Virasoro highest-weight classification]
\label{qthm:unitary-virasoro-minimal-classification}
Let \(L(c,h)\) be an irreducible positive-energy highest-weight module for the
Virasoro algebra with positive-definite Hermitian form and
\(L_n^\dagger=L_{-n}\).  Then either
\[
  c\ge1,\qquad h\ge0,
\]
or there is an integer \(m\ge3\) such that
\begin{equation}
  c=c_m:=1-\frac{6}{m(m+1)}
  \label{eq:unitary-minimal-central-charge}
\end{equation}
and
\begin{equation}
  h=h_{r,s}^{(m)}
  :=
  \frac{\bigl((m+1)r-ms\bigr)^2-1}{4m(m+1)}
  \label{eq:unitary-minimal-kac-weight}
\end{equation}
for integers
\[
  1\le r\le m-1,\qquad 1\le s\le m.
\]
The identification
\begin{equation}
  h_{r,s}^{(m)}=h_{m-r,m+1-s}^{(m)}
  \label{eq:kac-table-identification}
\end{equation}
leaves \(m(m-1)/2\) distinct irreducible modules.  The rational unitary
Virasoro minimal model \(\mathcal M(m,m+1)\) has precisely these simple
chiral modules.
\end{quotedtheorem}

The theorem is the deep unitarity input.  The monograph uses it as a
classification boundary, not as a substitute for the local representation
calculations above.  A convenient nonredundant list of representatives is the
triangular set
\[
  1\le s\le r\le m-1.
\]

\begin{quotedtheorem}[Unitary minimal-model modular data]
\label{qthm:unitary-minimal-model-modular-data}
Let \(\mathcal M(m,m+1)\) be the diagonal unitary Virasoro minimal model
with \(m\ge3\).  Its chiral character vector is indexed by Kac-table orbits
\[
  [r,s]=[m-r,m+1-s],
  \qquad
  1\le r\le m-1,\quad 1\le s\le m,
\]
or equivalently by the triangular representatives \(1\le s\le r\le m-1\).
The modular transformation \(\tau\mapsto-1/\tau\) is represented by
\begin{equation}
  S_{(r,s),(r',s')}
  =
  2\sqrt{\frac{2}{m(m+1)}}\,
  (-1)^{1+sr'+rs'}
  \sin\!\left(\frac{\pi(m+1)rr'}{m}\right)
  \sin\!\left(\frac{\pi mss'}{m+1}\right),
  \label{eq:unitary-minimal-model-s-matrix}
\end{equation}
and \(\tau\mapsto\tau+1\) is represented by the diagonal matrix
\begin{equation}
  T_{(r,s),(r,s)}
  =
  \exp\!\left(2\pi\ii\left(h_{r,s}^{(m)}-\frac{c_m}{24}\right)\right).
  \label{eq:unitary-minimal-model-t-matrix}
\end{equation}
The formula for \(S\) is well-defined on Kac-table orbits, is unitary on the
orbit basis, and together with \(T\) gives the genus-one modular
representation on characters.
\end{quotedtheorem}

The label identification is a quotient, not a mnemonic.  The involution
\[
  J(r,s)=(m-r,m+1-s)
\]
has no fixed points: a fixed point would require \(2r=m\) and \(2s=m+1\),
which cannot hold simultaneously in integers.  Thus every orbit has two
members, and the factor \(2\sqrt{2/(m(m+1))}\) in
\eqref{eq:unitary-minimal-model-s-matrix} is the quotient normalization.  If
one starts from the product of the two sine matrices for \(SU(2)_{m-2}\) and
\(SU(2)_{m-1}\), restriction to the \(J\)-orbits multiplies the parent
normalization by \(\sqrt2\).  This is the finite normalization step that is
often hidden in physics derivations.

It is useful to state the resulting fusion rule without appealing to any
picture of fields.  Let \(n^{(k)}_{ab}{}^c\) be the \(SU(2)_k\) fusion
coefficient for \(0\le a,b,c\le k\):
\[
  n^{(k)}_{ab}{}^c=
  \begin{cases}
    1,& |a-b|\le c\le \min(a+b,2k-a-b)\ \text{and}\ a+b+c\in2\mathbb Z,\\
    0,& \text{otherwise}.
  \end{cases}
\]
For triangular representatives \(a=(r,s)\), \(b=(r',s')\), and
\(c=(r'',s'')\), the unitary minimal-model Verlinde coefficient is
\begin{equation}
\begin{split}
  N_{ab}{}^c
  &=
  n^{(m-2)}_{r-1,r'-1}{}^{r''-1}\,
  n^{(m-1)}_{s-1,s'-1}{}^{s''-1}
\\
  &\quad+
  n^{(m-2)}_{r-1,r'-1}{}^{m-r''-1}\,
  n^{(m-1)}_{s-1,s'-1}{}^{m-s''}.
\end{split}
  \label{eq:unitary-minimal-model-fusion-rule}
\end{equation}
The first term fuses to the displayed representative of the target orbit; the
second fuses to its \(J\)-image.

\begin{proposition}[Minimal-model fusion from the finite quotient]
\label{prop:unitary-minimal-model-fusion-check}
Assume the modular data of
Theorem~\ref{qthm:unitary-minimal-model-modular-data}.  The Verlinde formula,
derived later in this chapter from modular diagonalization and displayed in
\eqref{eq:verlinde-formula}, gives exactly
\eqref{eq:unitary-minimal-model-fusion-rule}.  In particular the coefficients
are nonnegative integers.
\end{proposition}

\begin{proof}
Let
\[
  S^{(k)}_{ab}=\sqrt{\frac{2}{k+2}}\,
  \sin\!\left(\frac{\pi(a+1)(b+1)}{k+2}\right)
\]
be the \(SU(2)_k\) sine matrix.  The finite sine orthogonality relation
\[
  \sum_{x=0}^k
  S^{(k)}_{ax}S^{(k)}_{bx}=\delta_{ab}
\]
is the ordinary orthogonality of sine functions on the finite interval.
Put
\[
  \theta_x=\frac{\pi(x+1)}{k+2},
  \qquad
  \chi_a(\theta)=\frac{\sin((a+1)\theta)}{\sin\theta}.
\]
Then \(S^{(k)}_{ax}=S^{(k)}_{0x}\chi_a(\theta_x)\), and the Verlinde
coefficient is the finite inner product
\begin{equation}
  n^{(k)}_{ab}{}^c
  =
  \sum_{x=0}^k
  \bigl(S^{(k)}_{0x}\bigr)^2\,
  \chi_a(\theta_x)\chi_b(\theta_x)\chi_c(\theta_x).
  \label{eq:su2-finite-character-inner-product}
\end{equation}
The ordinary \(SU(2)\) character identity gives
\begin{equation}
  \chi_a\chi_b
  =
  \sum_{\substack{|a-b|\le d\le a+b\\ a+b+d\ {\rm even}}}\chi_d .
  \label{eq:ordinary-su2-character-product}
\end{equation}
On the finite grid \(\theta_x\) there are the affine-wall relations
\begin{equation}
  \chi_{k+1}(\theta_x)=0,\qquad
  \chi_{k+1+r}(\theta_x)=-\chi_{k+1-r}(\theta_x)
  \quad (r\ge1),
  \label{eq:su2-affine-wall-character-relation}
\end{equation}
which follow from \((k+2)\theta_x=\pi(x+1)\).  Substituting
\eqref{eq:ordinary-su2-character-product} in
\eqref{eq:su2-finite-character-inner-product}, reducing all terms by
\eqref{eq:su2-affine-wall-character-relation}, and using finite
orthogonality gives
\begin{equation}
  n^{(k)}_{ab}{}^c
  =
  \begin{cases}
  1,& |a-b|\le c\le \min(a+b,2k-a-b),\quad a+b+c\in2\mathbb Z,\\
  0,& \text{otherwise}.
  \end{cases}
  \label{eq:su2k-fusion-from-finite-grid}
\end{equation}
Indeed, if \(a+b\le k\) no wall term occurs and this is the ordinary
\(SU(2)\) tensor-product interval.  If \(a+b>k\), the reflected terms across
the wall \(k+1\) cancel exactly the labels \(c>2k-a-b\), leaving the truncated
alcove interval displayed above.

For the minimal model, the matrix
\eqref{eq:unitary-minimal-model-s-matrix} is the signed product of the
\(SU(2)_{m-2}\) and \(SU(2)_{m-1}\) sine matrices, followed by the
fixed-point-free quotient by \(J\).  The sign is the simple-current selection
rule in the Verlinde sum.  Therefore the quotient coefficient is obtained from
the parent product by summing over the two representatives \(c\) and \(Jc\) of
the target orbit.  Written in triangular labels, this gives exactly
\eqref{eq:unitary-minimal-model-fusion-rule}.  Each summand is a product of
\(0\)-\(1\) \(SU(2)_k\) fusion coefficients, so the result is a nonnegative
integer.
\end{proof}

\subsection{Coulomb-Gas Blocks and Screening Charges}
\label{subsec:minimal-model-coulomb-gas}

There is a second, complementary description of Virasoro minimal-model chiral
blocks: a free chiral scalar with background charge, together with contour
integrals of screening fields.  This construction is useful only after its
domain of validity is stated carefully.  It constructs chiral blocks and
intertwining maps in a free-field resolution of Virasoro modules; it is not by
itself a definition of the full nonchiral CFT, nor does it replace the Hilbert
space positivity and sewing data discussed above.

Let \(\varphi(z)\) be a chiral scalar normalized by
\begin{equation}
  \varphi(z)\varphi(w)\sim-\log(z-w),
  \qquad
  \partial\varphi(z)\partial\varphi(w)\sim-\frac1{(z-w)^2}.
  \label{eq:coulomb-gas-free-scalar-ope}
\end{equation}
For a complex background-charge parameter \(\alpha_0\), define
\begin{equation}
  T_{\alpha_0}(z)
  =
  -\frac12:\partial\varphi\,\partial\varphi:(z)
  +\ii\alpha_0\partial^2\varphi(z).
  \label{eq:coulomb-gas-stress-tensor}
\end{equation}
For \(V_\alpha(z)=:e^{\ii\alpha\varphi(z)}:\), Wick contraction gives
\begin{equation}
  T_{\alpha_0}(z)V_\alpha(w)
  \sim
  \frac{h(\alpha)V_\alpha(w)}{(z-w)^2}
  +
  \frac{\partial V_\alpha(w)}{z-w},
  \qquad
  h(\alpha)=\frac12\alpha(\alpha-2\alpha_0),
  \label{eq:coulomb-gas-vertex-weight}
\end{equation}
and
\begin{equation}
  c=1-12\alpha_0^2.
  \label{eq:coulomb-gas-central-charge}
\end{equation}
The sign of the improvement term in
\eqref{eq:coulomb-gas-stress-tensor} is fixed by the convention
\eqref{eq:coulomb-gas-free-scalar-ope}; changing the sign of
\(\varphi\) changes \(\alpha\) and \(\alpha_0\) together and leaves
\eqref{eq:coulomb-gas-vertex-weight} invariant.

For the unitary model \(\mathcal M(m,m+1)\), set
\begin{equation}
  \alpha_+^2=\frac{2(m+1)}{m},
  \qquad
  \alpha_-^2=\frac{2m}{m+1},
  \qquad
  \alpha_+\alpha_-=-2,
  \qquad
  \alpha_0=\frac{\alpha_++\alpha_-}{2}.
  \label{eq:coulomb-gas-unitary-alpha}
\end{equation}
Thus \(m\alpha_+ +(m+1)\alpha_-=0\).  The charge associated with the Kac label
\((r,s)\) is
\begin{equation}
  \alpha_{r,s}
  =
  \frac{1-r}{2}\alpha_+
  +
  \frac{1-s}{2}\alpha_- .
  \label{eq:coulomb-gas-kac-charge}
\end{equation}
Substitution into \eqref{eq:coulomb-gas-central-charge} and
\eqref{eq:coulomb-gas-vertex-weight} gives
\begin{equation}
  c=1-\frac{6}{m(m+1)}=c_m,
  \qquad
  h(\alpha_{r,s})
  =
  \frac{\bigl((m+1)r-ms\bigr)^2-1}{4m(m+1)}
  =
  h_{r,s}^{(m)}.
  \label{eq:coulomb-gas-kac-weight}
\end{equation}
The Kac reflection is charge conjugation in the background-charge scalar:
\[
  \alpha_{m-r,m+1-s}=2\alpha_0-\alpha_{r,s},
  \qquad
  h(2\alpha_0-\alpha)=h(\alpha).
\]

\begin{definition}[Screening currents and Coulomb-gas block datum]
\label{def:coulomb-gas-screening-block-datum}
The screening currents are
\begin{equation}
  S_\pm(u)=V_{\alpha_\pm}(u).
  \label{eq:coulomb-gas-screening-currents}
\end{equation}
Because \(\alpha_+\alpha_-=-2\), \eqref{eq:coulomb-gas-vertex-weight} gives
\(h(\alpha_+)=h(\alpha_-)=1\).  Given external charges
\(\beta_i\), insertion points \(z_i\), and nonnegative integers
\(N_\pm\), a genus-zero Coulomb-gas block datum consists of a twisted cycle
\(\Gamma\) for the multivalued integrand below and the neutrality condition
\begin{equation}
  \sum_i\beta_i+N_+\alpha_+ +N_-\alpha_- =2\alpha_0.
  \label{eq:coulomb-gas-neutrality}
\end{equation}
The corresponding chiral integral is
\begin{equation}
\begin{split}
  \mathcal F_\Gamma(z_i)
  &=
  \int_\Gamma
  \prod_{a=1}^{N_+}\dd u_a
  \prod_{b=1}^{N_-}\dd v_b\,
  \prod_{i<j}(z_i-z_j)^{\beta_i\beta_j}
\\
  &\quad\times
  \prod_{a,i}(u_a-z_i)^{\alpha_+\beta_i}
  \prod_{b,i}(v_b-z_i)^{\alpha_-\beta_i}
  \prod_{a<a'}(u_a-u_{a'})^{\alpha_+^2}
  \prod_{b<b'}(v_b-v_{b'})^{\alpha_-^2}
  \prod_{a,b}(u_a-v_b)^{-2}.
\end{split}
  \label{eq:coulomb-gas-block-integral}
\end{equation}
\end{definition}

\begin{proposition}[Screenings produce Virasoro intertwiners]
\label{prop:coulomb-gas-screening-intertwiners}
For any closed contour or twisted cycle \(\Gamma\) on which boundary terms
vanish, insertion of the screening charges
\[
  Q_\pm(\Gamma)=\int_\Gamma S_\pm(u)\,\dd u
\]
commutes with the Virasoro action generated by \(T_{\alpha_0}\).  Consequently
\eqref{eq:coulomb-gas-block-integral}, when convergent or defined by analytic
continuation from a convergent chamber, satisfies the Virasoro Ward identities
for a chiral block with external weights \(h(\beta_i)\).
\end{proposition}

\begin{proof}
Since \(S_\pm\) have weight \(1\),
\[
  T(z)S_\pm(u)
  \sim
  \frac{S_\pm(u)}{(z-u)^2}
  +
  \frac{\partial_u S_\pm(u)}{z-u}.
\]
The commutator of \(L_n=\oint z^{n+1}T(z)\dd z/(2\pi\ii)\) with the local
field \(S_\pm(u)\) is therefore
\[
  [L_n,S_\pm(u)]
  =
  \partial_u\!\left(u^{n+1}S_\pm(u)\right).
\]
Integrating over \(\Gamma\) gives zero because \(\Gamma\) is closed in the
ordinary case, and because the twisted-cycle boundary terms cancel with the
local-system monodromy in the multivalued case.  Thus the screening charges
are Virasoro intertwiners.  The free scalar correlator without screenings is
computed by Wick contraction and obeys the Virasoro Ward identity for
\(T_{\alpha_0}\) provided the total charge is \(2\alpha_0\); inserting
screening charges preserves the Ward identity and changes the total charge by
\(N_+\alpha_+ +N_-\alpha_-\).  This is exactly the neutrality condition
\eqref{eq:coulomb-gas-neutrality}.
\end{proof}

\begin{proposition}[One-screening Dotsenko--Fateev beta integral]
\label{prop:dotsenko-fateev-one-screening-beta}
Let \(A,B\in\mathbb C\) with initially
\(\operatorname{Re}A>-1\), \(\operatorname{Re}B>-1\), and choose the principal
branch on \((0,1)\).  Then the one-screening integral
\begin{equation}
  I(A,B)=\int_0^1 u^A(1-u)^B\,\dd u
  \label{eq:dotsenko-fateev-beta-integral}
\end{equation}
is
\begin{equation}
  I(A,B)
  =
  \frac{\Gamma(A+1)\Gamma(B+1)}
       {\Gamma(A+B+2)}.
  \label{eq:dotsenko-fateev-beta-result}
\end{equation}
Both sides define the meromorphic continuation of the same chiral block
coordinate in \(A,B\), with poles exactly where endpoint subtractions or
resonant descendants have to be specified.
\end{proposition}

\begin{proof}
In the convergence chamber this is Euler's beta integral: write
\[
  \Gamma(A+1)\Gamma(B+1)
  =
  \int_0^\infty\!\!\int_0^\infty
  e^{-x-y}x^A y^B\,\dd x\,\dd y.
\]
Use the change of variables \(t=x+y\), \(u=x/(x+y)\).  The Jacobian is
\(t\), so the double integral factors as
\[
  \left(\int_0^\infty e^{-t}t^{A+B+1}\dd t\right)
  \left(\int_0^1u^A(1-u)^B\dd u\right)
  =
  \Gamma(A+B+2)I(A,B).
\]
Division by \(\Gamma(A+B+2)\) gives
\eqref{eq:dotsenko-fateev-beta-result}.  Meromorphic continuation follows
because the gamma-function expression is meromorphic and agrees with the
integral on a connected open convergence chamber.
\end{proof}

\begin{remark}[What the Dotsenko--Fateev formulas do and do not prove]
\label{rem:dotsenko-fateev-status}
Multiple screening integrals are Selberg-type generalizations of
\eqref{eq:dotsenko-fateev-beta-integral}.  Their contour choices span spaces
of solutions to the BPZ differential equations and are a powerful way to
compute chiral-block connection matrices and selected structure constants.
The load-bearing data are the charges, the neutrality equation, the twisted
cycle, and the analytic continuation prescription.  A single displayed
integral, without those data and without the nonchiral sewing pairing, is not
a construction of the full minimal model.  Conversely, once the Hilbert space,
null quotient, modular data, and sewing constraints have been fixed, the
Coulomb-gas integrals give concrete coordinates on the same chiral block
spaces and make many BPZ solutions explicitly computable.
\end{remark}

Consequently the diagonal A-series full CFT has genus-one partition function
\[
  Z_m(\tau,\bar\tau)
  =
  \sum_{1\le s\le r\le m-1}
  \left|\chi_{r,s}(\tau)\right|^2,
\]
which is modular invariant because the same unitary \(S\) and diagonal \(T\)
act on the holomorphic and antiholomorphic character vectors.

For \(m=3\), one obtains
\[
  c_3=\frac12,\qquad
  h_{1,1}=0,\qquad h_{2,1}=\frac12,\qquad h_{2,2}=\frac1{16}.
\]
The same spin field may also be represented as \(h_{1,2}=1/16\), which is the
label making its level-two null vector manifest.  For
\(c=1/2\) and \(h=1/16\), the matrix
\eqref{eq:virasoro-level-two-gram} has determinant zero and
\eqref{eq:level-two-virasoro-null-vector} becomes
\begin{equation}
  \left(L_{-2}-\frac43 L_{-1}^2\right)\ket\sigma=0.
  \label{eq:ising-sigma-level-two-null}
\end{equation}

\begin{proposition}[Ising spin four-point function from the BPZ equation]
\label{prop:ising-spin-bpz-four-point}
Normalize the Ising spin field by
\(\langle\sigma(z,\bar z)\sigma(0)\rangle=|z|^{-1/4}\).  Let
\[
  \langle\sigma(z_1,\bar z_1)\cdots\sigma(z_4,\bar z_4)\rangle
  =
  |z_{12}|^{-1/4}|z_{34}|^{-1/4}f(z,\bar z)
\]
in the usual four-point cross-ratio frame.  The null vector
\eqref{eq:ising-sigma-level-two-null} implies the holomorphic BPZ equation
\begin{equation}
  \left[
    \partial_z^2
    +\frac{2-5z}{4z(1-z)}\partial_z
    -\frac{3}{64(1-z)^2}
  \right]f(z,\bar z)=0,
  \label{eq:ising-sigma-bpz-equation}
\end{equation}
and the analogous antiholomorphic equation.  A basis of local holomorphic
solutions is
\[
  f_\pm(z)=(1-z)^{-1/8}\sqrt{1\pm\sqrt z}.
\]
If
\[
  \mathcal F_0(z)=\frac{f_+(z)+f_-(z)}2,\qquad
  \mathcal F_{\varepsilon}(z)=f_+(z)-f_-(z),
\]
then single-valuedness, normalization, and crossing fix
\begin{equation}
  f(z,\bar z)
  =
  |\mathcal F_0(z)|^2+\frac14|\mathcal F_{\varepsilon}(z)|^2,
  \qquad
  C_{\sigma\sigma\varepsilon}=\frac12
  \label{eq:ising-spin-four-point-structure-constant}
\end{equation}
up to the sign convention for \(\varepsilon\).
\end{proposition}

\begin{proof}
The BPZ equation follows from the null vector as follows.  Insert
\eqref{eq:ising-sigma-level-two-null} at one point in the four-point
function.  The \(L_{-1}^2\) term is the second derivative with respect to
that point.  The \(L_{-2}\) term is computed by writing \(L_{-2}\) as a
small contour integral of \(T(\zeta)\), expanding the contour to surround the
other three insertions, and using the primary Ward identity
\[
  T(\zeta)\sigma(z_i)
  \sim
  \frac{(1/16)\sigma(z_i)}{(\zeta-z_i)^2}
  +
  \frac{\partial_{z_i}\sigma(z_i)}{\zeta-z_i}.
\]
Fixing three points by \(PSL_2(\mathbb C)\) covariance reduces the resulting
Ward identity to \eqref{eq:ising-sigma-bpz-equation}.  Direct substitution
checks that \(f_+\) and \(f_-\) solve the second-order equation.

The two solutions are multivalued, but the full correlator must be
single-valued.  Around \(z=0\), the combinations
\(\mathcal F_0\) and \(\mathcal F_{\varepsilon}\) have the OPE behavior of
the identity and energy channels in
\(\sigma\times\sigma=\mathbf1\oplus\varepsilon\).  Therefore the most general
diagonal single-valued expression in this basis is
\[
  f(z,\bar z)=|\mathcal F_0(z)|^2+C^2|\mathcal F_\varepsilon(z)|^2
\]
with \(C^2\ge0\) by reflection positivity.  The holomorphic solutions obey
the reduced crossing transformation
\[
  f_+(1-z)
  =
  \left(\frac{1-z}{z}\right)^{1/8}
  \frac{f_+(z)+f_-(z)}{\sqrt2},
  \qquad
  f_-(1-z)
  =
  \left(\frac{1-z}{z}\right)^{1/8}
  \frac{f_+(z)-f_-(z)}{\sqrt2}.
\]
The scalar prefactor is not part of the fusion matrix: it is the conformal
weight factor coming from the external spin fields.  In the cross-ratio frame
\(G(z,\bar z)=|z|^{-1/4}f(z,\bar z)\), crossing says
\(|1-z|^{-1/4}f(1-z,1-\bar z)=|z|^{-1/4}f(z,\bar z)\), and this exactly
cancels the modulus square of \(((1-z)/z)^{1/8}\).  Thus the matrix part is
the invariant datum.
Equivalently,
\[
  \mathcal F_0(1-z)
  =
  \left(\frac{1-z}{z}\right)^{1/8}
  \left(
  \frac1{\sqrt2}\mathcal F_0(z)
  +\frac1{2\sqrt2}\mathcal F_\varepsilon(z)
  \right),
\]
\[
  \mathcal F_\varepsilon(1-z)
  =
  \left(\frac{1-z}{z}\right)^{1/8}
  \left(
  \sqrt2\,\mathcal F_0(z)
  -\frac1{\sqrt2}\mathcal F_\varepsilon(z)
  \right).
\]
The remaining crossing condition for the matrix part then forces the
coefficient of the mixed term
\(\mathcal F_0\overline{\mathcal F_\varepsilon}+\mathrm{c.c.}\) to vanish:
\[
  \frac12-2C^2=0.
\]
Thus \(C^2=1/4\).  The coefficients of
\(|\mathcal F_0|^2\) and \(|\mathcal F_\varepsilon|^2\) then also match,
giving \eqref{eq:ising-spin-four-point-structure-constant}.
\end{proof}

\section{Sewing and Higher-Genus Blocks}
\label{sec:voa-higher-genus-sewing}

Sewing is the operation that constructs higher-genus surfaces and higher-point
blocks from lower-genus pieces.  Let \(C_1\) and \(C_2\) have marked points
with coordinates \(z\) and \(w\).  Removing disks and imposing
\[
  zw=q,\qquad |q|<1,
\]
glues the two punctures into an annulus.  If the two glued punctures carry a
module \(M_a\) and its dual \(M_a^\vee\), a sewn chiral block has the form
\begin{equation}
  \mathcal F_{\rm sewn}(q)
  =
  \sum_{\alpha}
  q^{h_a+n_\alpha-c/24}
  \mathcal F_1(\cdots,e_\alpha)
  \mathcal F_2(e^\alpha,\cdots).
  \label{eq:chiral-block-sewing}
\end{equation}
Here \(\{e_\alpha\}\) is a homogeneous basis of \(M_a\), \(e^\alpha\) is the
dual basis with respect to the invariant pairing, and
\(L_0 e_\alpha=(h_a+n_\alpha)e_\alpha\).  The exponent records propagation
along the sewn annulus.

The formula is meaningful only after specifying a topology on the module and
a convergence domain for the sum.  For a unitary positive-energy chiral
module \(M_a\) write
\[
  M_a=\widehat{\bigoplus_{n\ge0}}\,M_{a,h_a+n},
  \qquad
  d_a(n)=\dim M_{a,h_a+n},
\]
where \(L_0\) has eigenvalue \(h_a+n\) on \(M_{a,h_a+n}\).  For
\(0<|q|<1\) define the annular propagation operator
\begin{equation}
  P_a(q)=q^{L_0-c/24},
  \qquad
  P_a(q)e=q^{h_a+n-c/24}e
  \quad(e\in M_{a,h_a+n}),
  \label{eq:annular-propagator}
\end{equation}
with a fixed branch of \(\log q\) on the sewing coordinate chart.  Its trace
norm is
\begin{equation}
  \|P_a(q)\|_1
  =
  |q|^{h_a-c/24}\sum_{n\ge0}d_a(n)|q|^n
  =
  |\chi_a(q)|,
  \label{eq:annular-propagator-trace-norm}
\end{equation}
up to the phase carried by \(q^{L_0-c/24}\).  Thus the first analytic
condition behind sewing is ordinary character convergence at the modulus
\(|q|\).

\begin{definition}[Hilbert sewing datum]
\label{def:hilbert-sewing-datum}
Let \(M_a\) be a unitary chiral module with nondegenerate invariant Hermitian
pairing.  A one-channel Hilbert sewing datum consists of:
\begin{enumerate}
  \item two continuous linear functionals
  \(\ell_1,\ell_2\) on the Hilbert completion of \(M_a\), representing the
  two lower-genus chiral blocks with one \(M_a\)-leg and one \(M_a^\vee\)-leg;
  \item a sewing parameter \(q\) for which the propagator \(P_a(q)\) is
  trace class;
  \item a choice of dual homogeneous bases
  \(\{e_\alpha\}\), \(\{e^\alpha\}\) satisfying
  \(\langle e^\alpha,e_\beta\rangle=\delta^\alpha{}_\beta\).
\end{enumerate}
The sewn block is the scalar
\begin{equation}
  \operatorname{Sew}_{a,q}(\ell_1,\ell_2)
  =
  \sum_\alpha
  \ell_1(e_\alpha)\,
  q^{L_0(e_\alpha)-c/24}\,
  \ell_2(e^\alpha),
  \label{eq:hilbert-sewing-scalar}
\end{equation}
provided the series is absolutely convergent.
\end{definition}

\begin{proposition}[Trace-class estimate for one-channel sewing]
\label{prop:trace-class-estimate-one-channel-sewing}
Assume \(P_a(q)\) is trace class and that
\(\ell_i(v)=\langle u_i,v\rangle\) for vectors \(u_i\) in the Hilbert
completion of \(M_a\).  Then the series
\eqref{eq:hilbert-sewing-scalar} is absolutely convergent and satisfies
\begin{equation}
  \left|
    \operatorname{Sew}_{a,q}(\ell_1,\ell_2)
  \right|
  \le
  \|u_1\|\,\|u_2\|\,\|P_a(q)\|_1 .
  \label{eq:one-channel-sewing-bound}
\end{equation}
In particular the sewn block is independent of the chosen orthonormal
homogeneous basis.
\end{proposition}

\begin{proof}
Choose the homogeneous basis orthonormal.  With
\(\lambda_\alpha=q^{L_0(e_\alpha)-c/24}\),
\[
  \operatorname{Sew}_{a,q}(\ell_1,\ell_2)
  =
  \sum_\alpha
  \langle u_1,e_\alpha\rangle\lambda_\alpha
  \langle e_\alpha,u_2\rangle
  =
  \langle u_1,P_a(q)u_2\rangle
\]
when the finite-level partial sums are taken first.  Since \(P_a(q)\) is
trace class, it is bounded and
\(|\langle u_1,P_a(q)u_2\rangle|\le\|u_1\|\,\|P_a(q)\|\,\|u_2\|\le
\|u_1\|\,\|P_a(q)\|_1\,\|u_2\|\).  Absolute convergence follows from
\[
  \sum_\alpha
  |\langle u_1,e_\alpha\rangle|\,|\lambda_\alpha|\,
  |\langle e_\alpha,u_2\rangle|
  \le
  \|u_1\|\,\|u_2\|
  \sup_{\|v\|=\|w\|=1}
  \sum_\alpha |\lambda_\alpha|
  |\langle v,e_\alpha\rangle|\,|\langle e_\alpha,w\rangle|
  \le
  \|u_1\|\,\|u_2\|\sum_\alpha|\lambda_\alpha|.
\]
The last sum is \(\|P_a(q)\|_1\).  Since the value is the Hilbert inner
product \(\langle u_1,P_a(q)u_2\rangle\), changing the orthonormal basis
does not change the scalar.
\end{proof}

\begin{remark}[Torus trace from self-sewing]
\label{rem:torus-trace-from-self-sewing}
Let \(M_a\) be a unitary positive-energy module and let \(v\) be a chiral
state whose vertex operator \(Y(v,1)\) preserves the common domain of
\(P_a(q)\).  Sewing the two punctures of the sphere two-point block
\(\langle e^\alpha,Y(v,1)e_\alpha\rangle\) gives
\begin{equation}
  Z_a(v;q)
  =
  \operatorname{Tr}_{M_a}
  \left(
    Y(v,1)q^{L_0-c/24}
  \right).
  \label{eq:torus-one-point-trace-from-sewing}
\end{equation}
For \(v=\mathbf 1\), this is the character
\begin{equation}
  Z_a(\mathbf 1;q)=\chi_a(q)
  =
  \operatorname{Tr}_{M_a}q^{L_0-c/24}.
  \label{eq:character-from-self-sewing}
\end{equation}
Indeed, if \(\{e_\alpha\}\) is a homogeneous orthonormal basis, the self-sewn
block is
\[
  \sum_\alpha
  q^{L_0(e_\alpha)-c/24}
  \langle e_\alpha,Y(v,1)e_\alpha\rangle,
\]
which is exactly the trace of
\(Y(v,1)q^{L_0-c/24}\) on finite-level truncations and hence, whenever the
trace is convergent, the operator trace.  Setting \(v=\mathbf 1\) gives
\(Y(\mathbf 1,1)=\operatorname{id}_{M_a}\).
\end{remark}

\begin{definition}[Sewing graph for a chiral block]
\label{def:chiral-sewing-graph}
A chiral sewing graph \(\Gamma\) consists of:
\begin{enumerate}
  \item vertices \(v\), each labelled by a genus \(g_v\), external insertion
  labels, and a finite-dimensional chiral block functional
  \(\mathcal F_v\);
  \item internal edges \(e\), each joining two marked local coordinates or
  two marked coordinates on the same vertex surface;
  \item for every internal edge \(e\), a channel label \(a_e\), a pair of
  dual chiral modules \(M_{a_e}\), \(M_{a_e}^\vee\), and a sewing parameter
  \(q_e\) with \(|q_e|<1\);
  \item an orientation convention specifying which end of each edge carries
  \(M_{a_e}\) and which carries \(M_{a_e}^\vee\).
\end{enumerate}
Its geometric genus is
\begin{equation}
  g(\Gamma)=
  \sum_v g_v+b_1(\Gamma),
  \label{eq:sewing-graph-genus}
\end{equation}
where \(b_1(\Gamma)\) is the first Betti number of the graph.
\end{definition}

Given \(\Gamma\), the formal sewn block is obtained by inserting a dual basis
on every internal edge and multiplying the propagator factors:
\begin{equation}
  \mathcal F_\Gamma(\{q_e\})
  =
  \sum_{\{\alpha_e\}}
  \left(
    \prod_{e}
    q_e^{L_0(e_{\alpha_e})-c/24}
  \right)
  \prod_v
  \mathcal F_v(\ldots,e_{\alpha_e},e^{\alpha_e},\ldots).
  \label{eq:sewing-graph-block}
\end{equation}
The genus formula \eqref{eq:sewing-graph-genus} follows because sewing two
connected components adds genera, while sewing two punctures on the same
connected component adds one independent cycle.

\paragraph{Finite-channel sewing estimate.}
Assume a rational unitary chiral theory with finitely many channel labels,
and fix a sewing graph \(\Gamma\).  Suppose that for each edge \(e\),
\(P_{a_e}(q_e)\) is trace class, and that every vertex block functional
\(\mathcal F_v\) extends continuously to the Hilbert tensor product of its
incident channel modules.  Then the edge-basis sum
\eqref{eq:sewing-graph-block} converges absolutely for the chosen
\(\{q_e\}\).  Its value is independent of the orthonormal homogeneous bases
used on internal edges.

Insert finite-level projectors \(E_{a_e,\le N}\) on every internal edge.  For
fixed cutoffs the sum is finite.  The continuous vertex functionals have
finite operator norms on the tensor products of the incident Hilbert spaces.
Repeatedly applying the one-edge estimate
\eqref{eq:one-channel-sewing-bound} bounds the absolute value of the cutoff
sum by
\[
  \left(\prod_v \|\mathcal F_v\|\right)
  \left(\prod_e \|P_{a_e}(q_e)\|_1\right).
\]
The right hand side is finite by the trace-class hypotheses and independent
of the cutoffs.  Monotone convergence applied to the absolute values of the
finite basis sums gives absolute convergence.  Basis independence follows
edge by edge from Proposition~\ref{prop:trace-class-estimate-one-channel-sewing}.

\begin{hypothesis}[Analytic sewing theorem for rational chiral data]
\label{hyp:rational-analytic-sewing-theorem}
For the rational chiral theories treated as fully constructed in this
chapter, the absolutely convergent local sewing series
\eqref{eq:sewing-graph-block} analytically continue over the appropriate
cover of the moduli space of pointed curves, assemble into finite-rank vector
bundles of chiral blocks, and are compatible with changes of pants
decomposition by the fusing, braiding, and modular moves.  The compatibility
maps obey the pentagon, hexagon, and genus-one modular identities.
\end{hypothesis}

This hypothesis is not a cosmetic assumption.  The trace-class estimates
above control a local plumbing coordinate chart near \(q_e=0\).  They do not
by themselves prove global analytic continuation, mapping-class-group
covariance, or equality of two different decompositions of the same Riemann
surface.  Those assertions are precisely the higher-genus sewing theorem.

\begin{definition}[Sewing-compatible rational chiral theory]
\label{def:sewing-compatible-rational-chiral-theory}
For this chapter, a rational chiral theory is a VOA \(V\) with finitely many
simple ordinary modules \(\{M_i\}_{i\in I}\), semisimple module category,
finite-dimensional conformal-block spaces, nondegenerate two-point pairings,
and convergent sewing expansions \eqref{eq:chiral-block-sewing} in a
neighborhood of \(q=0\), satisfying
Hypothesis~\ref{hyp:rational-analytic-sewing-theorem}.  The analytic
continuations of these blocks along the moduli space of pointed curves give a
projectively flat vector bundle of conformal blocks.
\end{definition}

The mapping class group acts on this bundle.  In genus zero the elementary
moves are associativity and braiding of OPE channels.  In genus one the
generators are
\[
  S:\tau\mapsto -1/\tau,\qquad
  T:\tau\mapsto\tau+1.
\]
The compatibility of sewing with these moves is the Moore--Seiberg system:
pentagon, hexagon, and modular identities for associativity, braiding, and
twist data.  The modern algebraic home of this system is a modular tensor
category when the module category is semisimple.

\section{Modular Tensor Category and Verlinde Formula}

Let \(\mathcal C\) be the semisimple ribbon category of modules of a rational
chiral theory.  Its simple objects are \(M_i\), with tensor product
\[
  M_i\boxtimes M_j\cong \bigoplus_k N_{ij}{}^k M_k .
\]
The matrices \((N_i)_j{}^k=N_{ij}{}^k\) are the fusion matrices.  The category
has a braiding, duals, and twists \(\theta_i=\ee^{2\pi\ii h_i}\).  Its
modular \(S\)-matrix is the Hopf-link pairing normalized so that the vacuum
label \(0\) corresponds to \(V\).  In a unitary theory \(S\) is unitary and
\[
  d_i:=\frac{S_{i0}}{S_{00}}
\]
is the quantum dimension of \(M_i\).

\begin{quotedtheorem}[Modularity of rational chiral characters]
\label{qthm:voa-character-modularity}
For a rational, \(C_2\)-cofinite, positive-energy VOA satisfying the analytic
finiteness hypotheses needed for sewing, the characters
\(\chi_i(\tau)\) of simple modules span a finite-dimensional representation
of \(SL(2,\mathbb Z)\):
\[
  \chi_i(-1/\tau)=\sum_j S_{ij}\chi_j(\tau),
  \qquad
  \chi_i(\tau+1)=\sum_j T_{ij}\chi_j(\tau).
\]
The representation agrees with the modular data of the semisimple module
category.
\end{quotedtheorem}

\paragraph{Verlinde formula from modular diagonalization.}
Assume the fusion matrices are simultaneously diagonalized by a unitary
matrix \(S\) with \(S_{0m}\ne0\):
\begin{equation}
  (N_i S)_{jm}
  =
  \frac{S_{im}}{S_{0m}}S_{jm}.
  \label{eq:fusion-diagonalization-by-s}
\end{equation}
Then
\begin{equation}
  N_{ij}{}^k
  =
  \sum_m
  \frac{S_{im}S_{jm}\overline{S_{km}}}{S_{0m}} .
  \label{eq:verlinde-formula}
\end{equation}

The coefficient \(N_{ij}{}^k\) is the \(j,k\) matrix element of \(N_i\):
\[
  N_{ij}{}^k=(N_i)_j{}^k.
\]
Equation~\eqref{eq:fusion-diagonalization-by-s} says
\[
  N_i
  =
  S D_i S^{-1},
  \qquad
  (D_i)_{mm}=\frac{S_{im}}{S_{0m}}.
\]
Since \(S\) is unitary, \(S^{-1}_{mk}=\overline{S_{km}}\).  Therefore
\[
  (N_i)_j{}^k
  =
  \sum_m S_{jm}\frac{S_{im}}{S_{0m}}\overline{S_{km}},
\]
which is \eqref{eq:verlinde-formula}.

This calculation separates the deep input from the algebra.  The deep input is
that the same \(S\)-matrix obtained by the modular transformation of torus
characters diagonalizes the fusion ring.  Once this is granted, the formula is
linear algebra.

\section{The Ising Chiral Theory}
\label{sec:ising-chiral-theory}

The Ising minimal model has central charge
\[
  c=\frac12
\]
and three simple modules:
\[
  \mathbf 1,\qquad \sigma,\qquad \varepsilon ,
\]
with conformal weights
\[
  h_{\mathbf 1}=0,\qquad h_\sigma=\frac1{16},\qquad
  h_\varepsilon=\frac12 .
\]
In the ordered basis \((\mathbf 1,\sigma,\varepsilon)\), the modular
\(S\)-matrix is
\begin{equation}
  S_{\rm Ising}
  =
  \frac12
  \begin{pmatrix}
    1 & \sqrt2 & 1\\
    \sqrt2 & 0 & -\sqrt2\\
    1 & -\sqrt2 & 1
  \end{pmatrix}.
  \label{eq:ising-modular-s}
\end{equation}
The corresponding \(T\)-matrix is diagonal:
\begin{equation}
  T_{\rm Ising}
  =
  \operatorname{diag}\left(
    \ee^{-\pi\ii/24},
    \ee^{\pi\ii/12},
    \ee^{23\pi\ii/24}
  \right),
  \label{eq:ising-modular-t}
\end{equation}
because \(T_{ii}=\ee^{2\pi\ii(h_i-c/24)}\) and \(c=1/2\).
It is real, unitary, and obeys \(S^2=1\), because every simple object is
self-dual.  The quantum dimensions are
\[
  d_{\mathbf 1}=1,\qquad d_\sigma=\sqrt2,\qquad d_\varepsilon=1,
  \qquad
  \mathcal D^2=\sum_i d_i^2=4.
\]
Applying \eqref{eq:verlinde-formula} gives the fusion rules
\[
  \varepsilon\boxtimes\varepsilon=\mathbf 1,\qquad
  \varepsilon\boxtimes\sigma=\sigma,\qquad
  \sigma\boxtimes\sigma=\mathbf 1\oplus\varepsilon .
\]
The verification is pure Verlinde algebra.  The identity \(S^2=1\) is a direct
matrix multiplication using
\((\sqrt2)^2=2\).  The vacuum row gives the quantum dimensions by
\(d_i=S_{i0}/S_{00}\).  For example,
\[
  N_{\sigma\sigma}{}^{\mathbf 1}
  =
  \sum_m\frac{S_{\sigma m}S_{\sigma m}S_{\mathbf 1m}}{S_{\mathbf 1m}}
  =
  S_{\sigma\mathbf 1}^2+S_{\sigma\sigma}^2+S_{\sigma\varepsilon}^2
  =
  \frac12+0+\frac12=1,
\]
and
\[
  N_{\sigma\sigma}{}^\sigma
  =
  \sum_m\frac{S_{\sigma m}S_{\sigma m}S_{\sigma m}}{S_{\mathbf 1m}}
  =
  \frac{\sqrt2}{2}-\frac{\sqrt2}{2}=0.
\]
The same substitution gives
\(N_{\sigma\sigma}{}^\varepsilon=1\),
\(N_{\varepsilon\varepsilon}{}^{\mathbf 1}=1\), and
\(N_{\varepsilon\sigma}{}^\sigma=1\), with all other coefficients fixed by
commutativity and the unit object.

\section{Operator-Algebraic Conformal Nets}
\label{sec:operator-algebraic-conformal-nets}

The VOA formalism starts from chiral fields as formal distributions and then
extracts algebraic consequences.  The operator-algebraic formalism starts
from bounded observables localized in intervals on the circle.  This
difference has operator-algebraic content.  Smeared chiral fields are usually unbounded
operators; commutation on a common invariant domain does not by itself imply
commutation of the generated von Neumann algebras.  A conformal net records
the bounded local algebras directly, and treats fields as affiliated
coordinates when such coordinates exist.

\begin{definition}[Chiral conformal net]
\label{def:chiral-conformal-net}
Let \(\mathcal I(S^1)\) be the set of connected, nonempty, nondense open
intervals \(I\subset S^1\).  A M\"obius covariant chiral local net consists
of a Hilbert space \(\Hilb\), a unit vector \(\Omega\in\Hilb\), a positive
energy unitary representation \(U\) of the M\"obius group on \(\Hilb\), and
von Neumann algebras
\[
  \mathcal A(I)\subset B(\Hilb),\qquad I\in\mathcal I(S^1),
\]
such that:
\begin{enumerate}
\item isotony:
  \(I_1\subset I_2\) implies \(\mathcal A(I_1)\subset\mathcal A(I_2)\);
\item locality:
  \(I_1\cap I_2=\varnothing\) implies
  \([\mathcal A(I_1),\mathcal A(I_2)]=0\);
\item covariance:
  \(U(\gamma)\mathcal A(I)U(\gamma)^*=\mathcal A(\gamma I)\);
\item positive energy:
  the generator of rotations has spectrum bounded below by \(0\);
\item vacuum:
  \(U(\gamma)\Omega=\Omega\), and
  \(\Omega\) is cyclic for
  \(\bigvee_{I\in\mathcal I(S^1)}\mathcal A(I)\).
\end{enumerate}
The net is irreducible if
\(\bigvee_I\mathcal A(I)=B(\Hilb)\).  It is a conformal net, or
diffeomorphism-covariant net, if \(U\) extends to a strongly continuous
projective positive-energy representation of
\(\operatorname{Diff}^+(S^1)\) satisfying the same covariance and the local
implementer condition: if a diffeomorphism is the identity on \(I\), its
implementer acts trivially on \(\mathcal A(I)\).
\end{definition}

The complement \(I'\) of an interval is the interior of
\(S^1\setminus I\).  Haag duality is the equality
\[
  \mathcal A(I)'=\mathcal A(I') .
\]
For irreducible M\"obius covariant nets on the circle it follows under the
standard vacuum-net hypotheses; in higher-dimensional AQFT it is a separate
condition, as in
Chapter~\ref{ch:volume-iv-superselection-sectors-locality-properties}.

\begin{definition}[Split property, strong additivity, and complete rationality]
\label{def:complete-rational-conformal-net}
Let \(\mathcal A\) be an irreducible M\"obius covariant net.
\begin{enumerate}
\item The split property means that whenever
  \(\overline I_1\subset I_2\) there is a type I factor \(N\) with
  \[
    \mathcal A(I_1)\subset N\subset\mathcal A(I_2).
  \]
\item Strong additivity means that if deleting one point from an interval
  \(I\) gives the two intervals \(I_1,I_2\), then
  \[
    \mathcal A(I)=\mathcal A(I_1)\vee\mathcal A(I_2).
  \]
\item Assume the split property.  Choose four intervals
  \(I_1,I_2,I_3,I_4\) in cyclic order and set \(E=I_1\cup I_3\).  The
  \(\mu\)-index is the Jones index
  \[
    \mu_{\mathcal A}=[\mathcal A(E')':\mathcal A(E)]
  \]
  when this index is finite and independent of the chosen four intervals.
\end{enumerate}
The net is completely rational if it has the split property, is strongly
additive, and has finite \(\mu_{\mathcal A}\).
\end{definition}

\begin{quotedtheorem}[Representation category of a completely rational net]
\label{qthm:complete-rational-net-mtc}
For a completely rational chiral conformal net, the finite-index localized
representations form a unitary modular tensor category.  The categorical
global dimension agrees with the \(\mu\)-index:
\[
  \mu_{\mathcal A}=\sum_{\rho\in\operatorname{Irr}(\mathcal A)} d_\rho^2,
\]
where \(d_\rho\) is the statistical dimension of the sector \(\rho\).
This is the Kawahigashi--Longo--M\"uger conformal-net rationality theorem.
The proof uses subfactor index theory and DHR localization, and is not
reproduced here.
\end{quotedtheorem}

The comparison with VOAs is therefore a comparison of two different
coordinates on chiral locality.  A VOA gives formal fields and mode algebra;
a net gives bounded interval algebras.  Passing from one to the other is an
analytic theorem, not a definition.

\begin{quotedtheorem}[Strongly local VOAs and conformal nets]
\label{qthm:voa-to-conformal-net}
Let \(V\) be a simple unitary VOA whose vertex operators obey polynomial
energy bounds.  If the smeared fields \(Y(a,f)\), \(a\in V\),
\(f\in C^\infty(S^1)\), generate mutually local von Neumann algebras
\[
  \mathcal A_V(I)
  =
  W^*\{\overline{Y(a,f)}:\operatorname{supp}f\subset I,\ a\in V\},
  \qquad I\in\mathcal I(S^1),
\]
then \(V\) is called strongly local and \(I\mapsto\mathcal A_V(I)\) is an
irreducible conformal net.  The net is independent, up to isomorphism, of
the normalized invariant scalar product on \(V\).

Conversely, the Fredenhagen--J\"or{\ss} reconstruction applied to
\(\mathcal A_V\) recovers the smeared vertex operators of \(V\).  More
generally, if an irreducible conformal net is generated by quasi-primary
reconstructed fields, those fields satisfy polynomial energy bounds, and the
\(L_0\)-eigenspaces are finite-dimensional, then the finite-energy subspace
\(\Hilb^{\mathrm{fin}}\) carries a simple unitary strongly local VOA
structure \(V\) with \(\mathcal A\simeq\mathcal A_V\).

This is the Carpi--Kawahigashi--Longo--Weiner VOA-to-net theorem and its
Fredenhagen--J\"or{\ss} converse under the stated analytic hypotheses.
\end{quotedtheorem}

\begin{proposition}[Ising net \(\mu\)-index and DHR fusion]
\label{prop:ising-net-mu-index}
Assume the unitary Ising VOA is taken in its strongly local realization and
that its associated conformal net is completely rational with sectors
\(\mathbf 1,\sigma,\varepsilon\).  Then
\[
  \mu_{\mathcal A_{\mathrm{Ising}}}=4,
\]
and the DHR fusion ring of the net agrees with the Ising Verlinde fusion
ring displayed above.
\end{proposition}

\begin{proof}
From the explicit Ising \(S\)-matrix \eqref{eq:ising-modular-s}, the quantum
dimensions are
\[
  d_{\mathbf 1}=1,\qquad d_\sigma=\sqrt2,\qquad d_\varepsilon=1.
\]
The quoted complete-rationality theorem gives
\[
  \mu_{\mathcal A_{\mathrm{Ising}}}
  =
  d_{\mathbf 1}^2+d_\sigma^2+d_\varepsilon^2
  =
  1+2+1=4.
\]
The same theorem identifies finite-index DHR sectors with the modular tensor
category of the net.  Under the VOA-to-net comparison theorem, the strongly
local Ising VOA and the Ising net have the same chiral sectors, so the DHR
fusion coefficients are the Verlinde coefficients already computed.
\end{proof}

\begin{openproblem}[Unqualified VOA/net equivalence]
\label{op:voa-net-equivalence-without-extra-hypotheses}
Remove or sharply weaken the analytic hypotheses in
Theorem~\ref{qthm:voa-to-conformal-net}.  In particular, determine whether
every simple unitary positive-energy VOA satisfying the specified finiteness
conditions is strongly local, and whether every irreducible conformal net of
finite chiral type is recovered from a strongly local VOA.  The monograph
uses the proved strongly local correspondence as a theorem boundary and does
not assume this unqualified equivalence.
\end{openproblem}

\section{Full CFT and the Modular Bootstrap}

A full local CFT is not a chiral theory alone.  In a rational diagonal case,
one constructs a torus partition function from left and right characters:
\begin{equation}
  Z(\tau,\bar\tau)
  =
  \sum_{i,j}M_{ij}\chi_i(\tau)\overline{\chi_j(\tau)} .
  \label{eq:rational-torus-partition-function}
\end{equation}
The matrix \(M\) must have nonnegative integer entries, \(M_{00}=1\), and must
commute with the modular representation:
\begin{equation}
  MS=SM,\qquad MT=TM .
  \label{eq:modular-invariant-matrix}
\end{equation}
These are genus-one sewing constraints.  Higher-genus sewing imposes further
compatibility between \(M\), chiral blocks, duality moves, and the pairing of
left and right chiral sectors.

\begin{proposition}[Ising genus-one invariant]
\label{prop:ising-diagonal-modular-invariant}
For the Ising chiral data of Section~\ref{sec:ising-chiral-theory},
restrict to full CFT
torus partition functions whose left and right chiral labels both lie in
\((\mathbf 1,\sigma,\varepsilon)\).  Let
\[
  M=(M_{ij})_{i,j\in\{\mathbf 1,\sigma,\varepsilon\}}
  \in \operatorname{Mat}_3(\mathbb Z_{\ge0})
\]
with \(M_{\mathbf 1\mathbf 1}=1\).  If \(M\) satisfies
\eqref{eq:modular-invariant-matrix} with
\eqref{eq:ising-modular-s} and \eqref{eq:ising-modular-t}, then \(M=I_3\).
Thus the only genus-one modular invariant with one vacuum in this chiral
Ising label set is the diagonal invariant
\begin{equation}
  Z_{\rm Ising}(\tau,\bar\tau)
  =
  |\chi_{\mathbf 1}|^2+|\chi_\sigma|^2+|\chi_\varepsilon|^2 .
  \label{eq:ising-diagonal-torus-partition-function}
\end{equation}
\end{proposition}

\begin{proof}
The \(T\)-constraint is already a spin-selection rule.  Since \(T\) is
diagonal,
\[
  (MT)_{ij}-(TM)_{ij}
  =
  M_{ij}\left(
    \ee^{2\pi\ii(h_j-c/24)}
    -
    \ee^{2\pi\ii(h_i-c/24)}
  \right).
\]
Therefore \(M_{ij}\ne0\) implies \(h_i-h_j\in\mathbb Z\).  For the Ising
weights
\[
  0,\qquad \frac1{16},\qquad \frac12,
\]
no two distinct weights differ by an integer.  Hence \(M\) is diagonal:
\[
  M=\operatorname{diag}(1,a,b),
  \qquad a,b\in\mathbb Z_{\ge0}.
\]
The \(S\)-constraint now gives, entry by entry,
\[
  (MS)_{\mathbf 1\sigma}=S_{\mathbf 1\sigma},
  \qquad
  (SM)_{\mathbf 1\sigma}=aS_{\mathbf 1\sigma}.
\]
Since \(S_{\mathbf 1\sigma}=1/\sqrt2\ne0\), one gets \(a=1\).  Similarly
\[
  (MS)_{\mathbf 1\varepsilon}=S_{\mathbf 1\varepsilon},
  \qquad
  (SM)_{\mathbf 1\varepsilon}=bS_{\mathbf 1\varepsilon},
\]
and \(S_{\mathbf 1\varepsilon}=1/2\ne0\), so \(b=1\).  Thus
\(M=I_3\), and \eqref{eq:ising-diagonal-torus-partition-function} follows
from \eqref{eq:rational-torus-partition-function}.
\end{proof}

\subsection{Torus One-Point Blocks and Verlinde Defect Lines}
\label{subsec:torus-one-point-blocks-defect-lines}

The partition function is the genus-one zero-point function.  A complete
genus-one discussion also needs one-point blocks and topological defect
lines.  Both are often drawn pictorially; here we spell out the algebraic
content because the assumptions are different from those used in the scalar
partition function.

Let \(V\) be a rational chiral theory satisfying
Definition~\ref{def:sewing-compatible-rational-chiral-theory}, and let
\(v\in V\) be homogeneous of conformal weight \(h_v\).  The zero mode
\[
  o(v):=v_{h_v-1}
\]
has \(L_0\)-degree zero.  The chiral torus one-point block on the simple
module \(M_i\) is
\begin{equation}
  F_i(v;\tau)
  :=
  \operatorname{Tr}_{M_i}
  o(v)\,q^{L_0-c/24}.
  \label{eq:chiral-torus-one-point-block}
\end{equation}
Only the zero mode can contribute to a trace over \(L_0\)-eigenspaces:
a mode of nonzero \(L_0\)-degree maps each finite-dimensional eigenspace to a
different eigenspace and therefore has zero diagonal trace.  For
\(v=\mathbf 1\), \(o(v)=1\) and \(F_i(\mathbf 1;\tau)=\chi_i(\tau)\).

\begin{quotedtheorem}[Zhu modular covariance of torus one-point blocks]
\label{qthm:zhu-one-point-modular-covariance}
Assume \(V\) is rational, \(C_2\)-cofinite, of CFT type, and has ordinary
simple modules \(M_i\).  For each homogeneous \(v\in V\), the finite vector
\((F_i(v;\tau))_i\) spans a representation of \(SL_2(\mathbb Z)\).  If \(v\)
is Virasoro primary of weight \(h_v\), then for
\(\gamma=\begin{psmallmatrix}a&b\\ c&d\end{psmallmatrix}\)
\begin{equation}
  F_i\!\left(v;\frac{a\tau+b}{c\tau+d}\right)
  =
  (c\tau+d)^{h_v}
  \sum_j \rho_v(\gamma)_{ij}F_j(v;\tau).
  \label{eq:zhu-one-point-modular-covariance}
\end{equation}
For \(v=\mathbf 1\), \(\rho_v\) is the character modular representation
used above.
\end{quotedtheorem}

The factor \((c\tau+d)^{h_v}\) is just the conformal weight factor for the
change of torus coordinate.  The theorem input is the finite-dimensionality
and holomorphic modular closure of the trace functions.  A full-CFT torus
one-point function of a bulk primary \(\Phi\) of weights
\((h,\bar h)\) is a sesquilinear pairing of the corresponding chiral
one-point-block vectors.  Modular covariance therefore requires an invariant
coefficient tensor for
\(\rho_\Phi(\gamma)\otimes \overline{\rho_\Phi(\gamma)}\), together with the
weight factor \((c\tau+d)^h(c\bar\tau+d)^{\bar h}\).  Thus torus one-point
functions constrain bulk OPE data; they are not determined by the scalar
partition function alone.

Topological defect lines give a second genus-one probe.  In a diagonal
rational full CFT built from a unitary modular tensor category
\(\mathcal C\), the Verlinde defects are labelled by simple objects
\(a\in I\).  Let
\[
  \mathcal H_{\rm diag}
  =
  \bigoplus_{i\in I}M_i\otimes \overline{M_i^\vee}
\]
be the diagonal Hilbert space.  Define \(D_a\) on the summand labelled by
\(i\) by
\begin{equation}
  D_a\big|_{M_i\otimes \overline{M_i^\vee}}
  =
  \lambda_a(i)\operatorname{id},
  \qquad
  \lambda_a(i):=\frac{S_{ai}}{S_{0i}} .
  \label{eq:verlinde-defect-eigenvalue}
\end{equation}
The denominator is nonzero in a unitary modular tensor category because
\(S_{0i}=d_i S_{00}\) and \(d_i>0\).

\begin{proposition}[Verlinde defect fusion and modular covariance]
\label{prop:verlinde-defect-fusion-modular-covariance}
For the diagonal rational full CFT above,
\begin{equation}
  D_aD_b=\sum_c N_{ab}{}^cD_c .
  \label{eq:verlinde-defect-fusion}
\end{equation}
Moreover, the torus trace with \(D_a\) wrapping the Euclidean-time cycle is
\begin{equation}
  Z_a^{\rm time}(\tau,\bar\tau)
  =
  \sum_i \lambda_a(i)\chi_i(\tau)\overline{\chi_i(\tau)} .
  \label{eq:temporal-verlinde-defect-torus}
\end{equation}
After the modular \(S\)-move it becomes the spatial-defect trace
\begin{equation}
  Z_a^{\rm time}(-1/\tau,-1/\bar\tau)
  =
  \sum_{p,q}N_{ap}{}^q\,\chi_p(\tau)\overline{\chi_q(\tau)} .
  \label{eq:spatial-verlinde-defect-torus}
\end{equation}
\end{proposition}

\begin{proof}
The Verlinde formula says that the matrices \(N_a\) are simultaneously
diagonalized by \(S\), with eigenvalue \(S_{ai}/S_{0i}\) on the \(i\)-th
character.  Hence for every \(i\),
\[
  \lambda_a(i)\lambda_b(i)
  =
  \sum_c N_{ab}{}^c\lambda_c(i),
\]
which proves \eqref{eq:verlinde-defect-fusion} on each diagonal summand of
\(\mathcal H_{\rm diag}\).

For the modular transform, insert the character transformation law into
\eqref{eq:temporal-verlinde-defect-torus}.  The coefficient of
\(\chi_p\overline{\chi_q}\) is
\[
  \sum_i
  \frac{S_{ai}}{S_{0i}}\,S_{ip}\,\overline{S_{iq}}
  =
  \sum_i
  \frac{S_{ai}S_{pi}S_{q^\vee i}}{S_{0i}}
  =
  N_{ap}{}^q,
\]
where unitarity gives \(\overline{S_{iq}}=S_{q^\vee i}\), and the last
equality is Verlinde.  This proves
\eqref{eq:spatial-verlinde-defect-torus}.  The right side is a Hilbert-space
trace over
\[
  \mathcal H_a
  =
  \bigoplus_{p,q}N_{ap}{}^q\,
  M_p\otimes\overline{M_q^\vee},
\]
so the modular move has turned a temporal defect operator into a spatial
defect sector.
\end{proof}

For the Ising labels \((\mathbf 1,\sigma,\varepsilon)\), the eigenvalue
vectors \((\lambda_a(\mathbf 1),\lambda_a(\sigma),\lambda_a(\varepsilon))\)
are
\[
  \lambda_{\mathbf 1}=(1,1,1),\qquad
  \lambda_\sigma=(\sqrt2,0,-\sqrt2),\qquad
  \lambda_\varepsilon=(1,-1,1).
\]
Thus
\[
  D_\varepsilon^2=D_{\mathbf 1},\qquad
  D_\varepsilon D_\sigma=D_\sigma,\qquad
  D_\sigma^2=D_{\mathbf 1}+D_\varepsilon,
\]
so \(D_\varepsilon\) is the invertible spin-flip line and \(D_\sigma\) is the
noninvertible Kramers--Wannier line.  Their modular covariance is completely
finite.  For example,
\[
  Z_\varepsilon^{\rm time}
  =
  |\chi_{\mathbf 1}|^2-|\chi_\sigma|^2+|\chi_\varepsilon|^2
\]
is sent by \(S\) to
\[
  \chi_{\mathbf 1}\overline{\chi_\varepsilon}
  +\chi_\varepsilon\overline{\chi_{\mathbf 1}}
  +|\chi_\sigma|^2,
\]
the Hilbert space twisted by the spin-flip defect.  Similarly
\[
  Z_\sigma^{\rm time}
  =
  \sqrt2\left(|\chi_{\mathbf 1}|^2-|\chi_\varepsilon|^2\right)
\]
is sent by \(S\) to
\[
  \chi_{\mathbf 1}\overline{\chi_\sigma}
  +\chi_\varepsilon\overline{\chi_\sigma}
  +\chi_\sigma\overline{\chi_{\mathbf 1}}
  +\chi_\sigma\overline{\chi_\varepsilon}.
\]
The same line-sliding argument gives selection rules for torus one-point
functions.  Since \(D_\varepsilon\) acts by \(-1\) on the Ising spin primary,
the torus one-point function of \(\sigma\) equals its negative and therefore
vanishes.  This topological-defect Ward identity is independent of any
particular lattice realization.

For a unitary compact two-dimensional CFT with discrete spectrum on the
circle, the rectangular torus partition function
\[
  Z(\beta)=\operatorname{Tr}_{\mathcal H(S^1)}
  \exp\{-\beta(H-c_{\rm tot}/24)\},
  \qquad c_{\rm tot}=c+\bar c,
\]
obeys modular invariance
\[
  Z(\beta)=Z(4\pi^2/\beta)
\]
when the spatial circle has circumference \(2\pi\).  If the low-temperature
trace is dominated by a unique vacuum, then as \(\beta\to0^+\),
\begin{equation}
  \log Z(\beta)
  =
  \frac{\pi^2 c_{\rm tot}}{6\beta}+o(\beta^{-1}).
  \label{eq:cardy-partition-leading}
\end{equation}
The derivation is only the modular transformation:
low temperature at \(4\pi^2/\beta\) gives
\[
  Z(4\pi^2/\beta)
  =
  \exp\left\{\frac{4\pi^2}{\beta}\frac{c_{\rm tot}}{24}\right\}
  (1+o(1)),
\]
which is \eqref{eq:cardy-partition-leading}.  Tauberian hypotheses convert
this high-temperature partition-function statement into an asymptotic density
of states.  Those hypotheses are analytic input; modular invariance alone is
not a pointwise formula for individual degeneracies.

\begin{quotedtheorem}[Exponential Tauberian theorem]
\label{qthm:exponential-tauberian-cardy}
Let \(N(E)\) be a nondecreasing right-continuous function on
\([0,\infty)\), and suppose its Laplace transform
\[
  Z(\beta)=\int_{[0,\infty)}\ee^{-\beta E}\,\dd N(E)
\]
is finite for every \(\beta>0\).  If, for some \(A>0\),
\[
  \log Z(\beta)=\frac A\beta+o(\beta^{-1})
  \qquad(\beta\downarrow0),
\]
then
\[
  \log N(E)=2\sqrt{AE}+o(\sqrt E)
  \qquad(E\to\infty).
\]
\end{quotedtheorem}

This theorem is the precise analytic bridge between the modular
high-temperature statement and the density of states.  Its positivity
hypothesis is essential: \(N(E)\) is a cumulative spectral counting function,
not a signed distribution obtained by subtracting sectors.

\begin{proposition}[CFT-internal Cardy growth]
\label{prop:cft-internal-cardy-growth}
Let \(\mathcal C\) be a unitary compact two-dimensional CFT on a circle of
circumference \(2\pi\).  Assume the Hilbert-space spectrum of
\[
  E=L_0+\bar L_0
\]
is discrete with finite multiplicities, \(E\ge0\), and has a unique vacuum at
\(E=0\).  Assume also that the torus partition function is modular invariant
and has no continuous spectral contribution.  Let
\[
  N(E)=\dim\{\psi\in\mathcal H(S^1)\mid
  (L_0+\bar L_0)\psi\le E\}
\]
be the cumulative degeneracy.  Then
\begin{equation}
  \log N(E)
  =
  2\pi\sqrt{\frac{c_{\rm tot}E}{6}}+o(\sqrt E),
  \qquad
  E\to\infty .
  \label{eq:cardy-cumulative-density}
\end{equation}
\end{proposition}

\begin{proof}
The shifted cylinder partition function used above is
\[
  Z(\beta)
  =
  \operatorname{Tr}\exp\{-\beta(L_0+\bar L_0-c_{\rm tot}/24)\}.
\]
Multiplying by \(\exp\{-\beta c_{\rm tot}/24\}\) gives the ordinary Laplace
transform of the positive counting measure \(\dd N(E)\):
\[
  \widetilde Z(\beta)
  =
  \operatorname{Tr}\exp\{-\beta(L_0+\bar L_0)\}
  =
  \int_{[0,\infty)}\ee^{-\beta E}\,\dd N(E).
\]
Since \(\beta c_{\rm tot}/24=o(\beta^{-1})\), the high-temperature asymptotic
\eqref{eq:cardy-partition-leading} gives
\[
  \log \widetilde Z(\beta)
  =
  \frac{\pi^2c_{\rm tot}}{6\beta}+o(\beta^{-1}).
\]
Apply Theorem~\ref{qthm:exponential-tauberian-cardy} with
\(A=\pi^2c_{\rm tot}/6\).
\end{proof}

A saddle calculation checks the coefficient but does not replace the
Tauberian theorem.  If a smooth density had entropy
\[
  S(E)=2\sqrt{AE},
\]
then the Laplace exponent \(S(E)-\beta E\) is stationary at
\[
  E_\ast=\frac A{\beta^2},
\]
and the stationary value is \(A/\beta\).  Thus the density-growth coefficient
in \eqref{eq:cardy-cumulative-density} is exactly the Legendre dual of the
modular coefficient in \eqref{eq:cardy-partition-leading}; the theorem above
is what justifies this conclusion for a discrete positive spectrum.

The modular bootstrap is the study of the constraints
\eqref{eq:modular-invariant-matrix}, reflection positivity, spin integrality,
and higher-genus sewing on the spectrum and OPE data.  In rational theories it
is finite-dimensional algebra.  In nonrational compact theories it is an
infinite-dimensional positivity and growth problem for torus and higher-genus
partition functions.

\section{Direct-Integral Data for Nonrational CFT}
\label{sec:nonrational-cft-direct-integral-data}

The rational theory discussed above has two simplifying properties: the
chiral sector category has finitely many simple objects, and every sewing
formula reduces to a finite sum.  Neither property is part of the local
definition of a two-dimensional conformal field theory.  A compact CFT may
have a countably infinite discrete spectrum, as in an irrational compact
boson.  A noncompact CFT may have continuous normalizable spectrum, as in
Liouville theory.  In both cases the finite sum over intermediate primaries
must be replaced by a spectral resolution of Hilbert-space vectors.

\begin{definition}[Spectral radial product datum]
\label{def:spectral-radial-product-datum}
Fix a unitary two-dimensional CFT on the round circle with Hilbert space
\(\mathcal H\), vacuum \(\Omega\), self-adjoint nonnegative operators
\(L_0,\bar L_0\), and dense local domain \(\mathcal D_{\rm loc}\).  A
spectral radial product datum for two local fields \(A,B\) consists of the
following data.
\begin{enumerate}
  \item For every \(0<r<1\) and \(\theta\in\mathbb R/2\pi\mathbb Z\), the
  radially ordered product vector
  \[
    \Phi_{BA}(r,\theta)=B(re^{\ii\theta})A(0)\Omega
  \]
  is a vector in \(\mathcal H\).
  \item Let \(\mathcal H_{BA}\subset\mathcal H\) be the closed subspace
  generated by all \(\Phi_{BA}(r,\theta)\) and by the action of the two
  Virasoro algebras.  The representation of the commuting spectral observables
  used to label intermediate sectors is represented as a direct integral
  \begin{equation}
    \mathcal H_{BA}
    \simeq
    \int_{X_{BA}}^{\oplus}\mathcal K_x\,\dd\mu_{BA}(x),
    \label{eq:direct-integral-ba}
  \end{equation}
  where \(X_{BA}\) is a standard Borel space, \(\mu_{BA}\) is a positive
  measure, and \(x\mapsto\mathcal K_x\) is a measurable field of Hilbert
  spaces carrying the descendant and multiplicity degrees of freedom.
  \item In this representation the product vector is a square-integrable
  measurable vector field
  \[
    \widehat\Phi_{BA}(x;r,\theta)\in\mathcal K_x,
    \qquad
    \int_{X_{BA}}\|\widehat\Phi_{BA}(x;r,\theta)\|_{\mathcal K_x}^2
      \dd\mu_{BA}(x)<\infty .
  \]
\end{enumerate}
The variable \(x\) is an intermediate spectral label, not necessarily the
label of a normalizable primary vector.  When \(x\) lies in a continuous
spectrum, the formal object called a primary of label \(x\) is a generalized
eigenvector; the Hilbert-space object is the square-integrable field
\(\widehat\Phi_{BA}\).
\end{definition}

This definition is deliberately phrased before choosing a basis of chiral
blocks.  The direct integral is supplied by the spectral theorem for the
commuting self-adjoint operators and by the decomposition of the chiral
representation on the closed cyclic subspace.  A conformal-block expansion is
obtained only after choosing measurable bases of intertwiners in the fibers
\(\mathcal K_x\).  The Plancherel measure and the multiplicity fields are
therefore part of the CFT data.

\begin{remark}[Direct-integral OPE identity]
\label{rem:direct-integral-ope-identity}
Let \(A,B,C,D\in\mathcal D_{\rm loc}\) and assume that the radial products
\(\Phi_{BA}(r,\theta)\) and \(\Phi_{CD}(\rho,\varphi)\) have spectral radial
product data using a common direct-integral decomposition
\[
  \mathcal H_{ABCD}
  \simeq
  \int_X^{\oplus}\mathcal K_x\,\dd\mu(x)
\]
of the closed subspace generated by the two product families.  Then the
four-point function in the radial domain \(0<r<\rho<1\) is
\begin{equation}
  \langle
    C(\rho e^{\ii\varphi})D(0)\Omega,\,
    B(re^{\ii\theta})A(0)\Omega
  \rangle_{\mathcal H}
  =
  \int_X
  \left\langle
    \widehat\Phi_{CD}(x;\rho,\varphi),\,
    \widehat\Phi_{BA}(x;r,\theta)
  \right\rangle_{\mathcal K_x}
  \dd\mu(x).
  \label{eq:direct-integral-four-point}
\end{equation}
If the measure is counting measure on a finite set this is the rational
finite sum over intermediate states.  If the measure has a continuous part,
the same formula is an \(L^2\)-inner product, not a pointwise sum over
delta-normalized states.
Indeed, choose the unitary map
\[
  U:\mathcal H_{ABCD}\longrightarrow
  \int_X^{\oplus}\mathcal K_x\,\dd\mu(x)
\]
implementing the direct integral.  By definition,
\(U\Phi_{BA}=\widehat\Phi_{BA}\) and
\(U\Phi_{CD}=\widehat\Phi_{CD}\).  Unitarity of \(U\) gives the Plancherel
identity
\[
  \langle \Phi_{CD},\Phi_{BA}\rangle_{\mathcal H}
  =
  \int_X
    \langle
      (U\Phi_{CD})(x),(U\Phi_{BA})(x)
    \rangle_{\mathcal K_x}\dd\mu(x),
\]
which is \eqref{eq:direct-integral-four-point}.  No assumption about a
discrete basis of normalizable primaries has entered the argument.
\end{remark}

The usual OPE notation is a compressed way of writing
\eqref{eq:direct-integral-four-point}.  For a continuous spectrum it should
not be read as a product identity in a set of fields.  It is an identity of
matrix elements after smearing the intermediate label against the
Plancherel measure and after specifying the domain of radial convergence.

\begin{definition}[Measurable fusing transform]
\label{def:measurable-fusing-transform}
For fixed ordered external fields \(A,B,C,D\), let
\[
  \int_{X_s}^{\oplus}\mathcal K_x^{(s)}\,\dd\mu_s(x),
  \qquad
  \int_{X_t}^{\oplus}\mathcal K_y^{(t)}\,\dd\mu_t(y)
\]
be the direct-integral Hilbert spaces obtained from the \(s\)-channel
radial grouping \((BA)(DC)\) and the \(t\)-channel grouping \((CB)(DA)\).
A measurable fusing transform is a unitary operator
\begin{equation}
  \mathbb F_{st}:
  \int_{X_s}^{\oplus}\mathcal K_x^{(s)}\,\dd\mu_s(x)
  \longrightarrow
  \int_{X_t}^{\oplus}\mathcal K_y^{(t)}\,\dd\mu_t(y)
  \label{eq:measurable-fusing-transform}
\end{equation}
which intertwines the two analytic continuations of the same chiral
four-point block from the \(s\)- and \(t\)-channel radial domains.  When the
fibers are finite-dimensional and a measurable basis has been chosen, this
operator is represented by a kernel
\begin{equation}
  (\mathbb F_{st} f)(y)
  =
  \int_{X_s}
  F_{st}(y,x)f(x)\,\dd\mu_s(x),
  \label{eq:fusing-kernel-direct-integral}
\end{equation}
understood as an equality in the direct-integral Hilbert space, with the
unitarity condition
\begin{equation}
  \int_{X_t} F_{st}(y,x)^\dagger F_{st}(y,x')\,\dd\mu_t(y)
  =
  \delta_{\mu_s}(x,x')\,\mathbf 1
  \label{eq:fusing-kernel-unitarity}
\end{equation}
in distributional notation.
\end{definition}

\paragraph{Plancherel form of crossing.}
Assume the hypotheses of Definition~\ref{def:measurable-fusing-transform}.
Let \(v_s,w_s\) be the two \(s\)-channel square-integrable coefficient fields
whose inner product gives the four-point function in an \(s\)-channel radial
domain, and let \(v_t,w_t\) be the corresponding \(t\)-channel coefficient
fields.  If
\begin{equation}
  v_t=\mathbb F_{st}v_s,\qquad w_t=\mathbb F_{st}w_s,
  \label{eq:coefficient-fields-fusing-transform}
\end{equation}
then unitarity of the fusing transform gives
\begin{equation}
  \int_{X_s}
    \langle w_s(x),v_s(x)\rangle_{\mathcal K_x^{(s)}}\,\dd\mu_s(x)
  =
  \int_{X_t}
    \langle w_t(y),v_t(y)\rangle_{\mathcal K_y^{(t)}}\,\dd\mu_t(y).
  \label{eq:direct-integral-crossing}
\end{equation}
In this formulation the displayed Hilbert-space identity is the final
Plancherel step.  The mathematical content is the construction of the
direct-integral decompositions, the proof that the conformal blocks admit the
unitary transform \(\mathbb F_{st}\), and the verification of
\eqref{eq:coefficient-fields-fusing-transform} for the actual OPE kernels.
In a rational CFT the same statement reduces to a finite matrix identity.  In
Liouville theory the label spaces are continuous and the identity becomes the
Ponsot--Teschner Virasoro fusion-kernel identity, together with the DOZZ
coefficient relation.

\begin{definition}[Nonrational genus-zero bootstrap problem]
\label{def:nonrational-genus-zero-bootstrap-problem}
A nonrational genus-zero bootstrap problem consists of:
\begin{enumerate}
  \item a class of local fields and their dense radial-product domain;
  \item for every ordered pair of fields, direct-integral intermediate
  spectra with Plancherel measures and multiplicity fibers;
  \item measurable three-point/OPE coefficient fields;
  \item chiral conformal blocks with specified radial convergence domains
  and analytic continuations;
  \item measurable fusing and braiding transforms satisfying unitarity,
  pentagon, and hexagon identities as identities of direct-integral
  operators;
  \item a reflection-positive pairing of left and right chiral data producing
  single-valued Euclidean correlation distributions.
\end{enumerate}
Solving this problem means proving existence, uniqueness up to unitary
equivalence of the resulting correlation theory, reflection positivity,
locality, and compatibility with sewing in the domains under consideration.
Crossing equations for individual four-point functions are necessary
consequences of such a solution, not a replacement for the direct-integral
and sewing data.
\end{definition}

\begin{example}[Two spectral types]
\label{ex:nonrational-cft-two-spectral-types}
For an irrational compact free boson the intermediate label space is
countable, the measure is counting measure, and modular covariance is governed
by theta functions and Poisson summation rather than by a finite modular
matrix.  For Liouville theory the normalizable bulk spectrum is
\[
  \alpha=\frac Q2+\ii P,\qquad P\in\mathbb R_{\ge0},
\]
with Plancherel measure fixed by the two-point normalization and reflection
amplitude.  The four-point function is the direct integral displayed in
Definition~\ref{def:liouville-continuous-blocks}.  These two examples have
different spectral measures, but both are governed by the same Hilbert-space
identity \eqref{eq:direct-integral-four-point}.
\end{example}

\section{Logarithmic Theories}

A logarithmic CFT is a conformal field theory whose chiral module category is
not semisimple and in which the energy operator \(L_0\) need not be
diagonalizable.  Diagonal \(L_0\) is what made the rational characters honest
sums of powers of \(q\)
over simple modules; a nilpotent part of \(L_0\) changes both local conformal
covariance and the genus-one trace problem.

\begin{definition}[Rank-two logarithmic Virasoro pair]
\label{def:rank-two-logarithmic-pair}
A rank-two logarithmic pair of chiral fields of weight \(h\) is a pair
\((\mathcal C,\mathcal D)\) whose states \(C,D\) satisfy
\[
  L_0C=hC,\qquad L_0D=hD+C,\qquad L_nC=L_nD=0\quad(n>0).
\]
Equivalently, the singular part of the stress-tensor OPE is
\[
  T(\zeta)\mathcal C(z)
  \sim
  \frac{h\mathcal C(z)}{(\zeta-z)^2}
  +
  \frac{\partial\mathcal C(z)}{\zeta-z},
\]
\[
  T(\zeta)\mathcal D(z)
  \sim
  \frac{h\mathcal D(z)+\mathcal C(z)}{(\zeta-z)^2}
  +
  \frac{\partial\mathcal D(z)}{\zeta-z}.
\]
Thus \(\mathcal C\) is primary, while \(\mathcal D\) is its logarithmic
partner.  With \(N D=C\) and \(NC=0\), the two-dimensional span of
\((C,D)\) carries \(L_0=h\operatorname{id}+N\).
\end{definition}

The finite consequences are immediate and worth recording because this is
where signs and \(2\pi\ii\)'s enter.  On states,
\[
  q^{L_0-c/24}C=q^{h-c/24}C,\qquad
  q^{L_0-c/24}D=q^{h-c/24}\bigl(D+2\pi\ii\tau\,C\bigr),
  \qquad q=\ee^{2\pi\ii\tau}.
\]
On fields, the Virasoro commutators are, for \(n=-1,0,1\),
\[
  [L_n,\mathcal C(z)]
  =
  \bigl(z^{n+1}\partial_z+(n+1)hz^n\bigr)\mathcal C(z),
\]
\[
  [L_n,\mathcal D(z)]
  =
  \bigl(z^{n+1}\partial_z+(n+1)hz^n\bigr)\mathcal D(z)
  +(n+1)z^n\mathcal C(z).
\]
In particular,
\[
  \ee^{sL_0}\mathcal C(z)\ee^{-sL_0}
  =
  \ee^{sh}\mathcal C(\ee^s z),
\]
\[
  \ee^{sL_0}\mathcal D(z)\ee^{-sL_0}
  =
  \ee^{sh}\bigl(\mathcal D(\ee^s z)+s\mathcal C(\ee^s z)\bigr).
\]
The additive \(s=\log\lambda\) term is the local origin of logarithms in
correlators.

\begin{proposition}[Two-point functions of a logarithmic pair]
\label{prop:logarithmic-pair-two-point-functions}
Assume the vacuum is invariant under \(L_{-1},L_0,L_1\), the two fields in
Definition~\ref{def:rank-two-logarithmic-pair} are mutually local, and their
two-point functions are single-valued on a chosen simply connected
punctured coordinate chart after a branch of \(\log z\) is fixed.  Then
translation, scaling, and special-conformal Ward identities force
\[
  \langle\mathcal C(z)\mathcal C(0)\rangle=0,
\]
\[
  \langle\mathcal C(z)\mathcal D(0)\rangle
  =
  b\,z^{-2h},
\]
\[
  \langle\mathcal D(z)\mathcal D(0)\rangle
  =
  (d-2b\log z)z^{-2h},
\]
for constants \(b,d\in\mathbb C\).  The constant \(d\) depends on the choice
of logarithmic partner \(D\), while \(b\) is invariant under
\(D\mapsto D+\lambda C\).
\end{proposition}

\begin{proof}
Translation invariance makes the correlators functions of
\(z=z_1-z_2\).  Write
\[
  G_{CC}(z)=\langle\mathcal C(z)\mathcal C(0)\rangle,\qquad
  G_{CD}(z)=\langle\mathcal C(z)\mathcal D(0)\rangle,
\]
\[
  G_{DD}(z)=\langle\mathcal D(z)\mathcal D(0)\rangle.
\]
The \(L_0\) Ward identity gives
\[
  (z\partial_z+2h)G_{CC}=0,
\]
\[
  (z\partial_z+2h)G_{CD}+G_{CC}=0,
\]
\[
  (z\partial_z+2h)G_{DD}+2G_{CD}=0.
\]
Hence, before imposing \(L_1\),
\[
  G_{CC}=A z^{-2h},\qquad
  G_{CD}=(B-A\log z)z^{-2h},
\]
\[
  G_{DD}=(D_0-2B\log z+A(\log z)^2)z^{-2h}.
\]
This calculation is the scale-covariant source of logarithms.

Now impose the \(L_1\) Ward identity on
\(\langle\mathcal C(z_1)\mathcal D(z_2)\rangle\).  The ordinary primary part
of the differential operator is
\[
  (z_1+z_2)(z\partial_z+2h)G_{CD}(z),
  \qquad z=z_1-z_2,
\]
and the nilpotent term from the \(\mathcal D\)-insertion is
\[
  2z_2G_{CC}(z).
\]
Using the \(L_0\) equation, the total \(L_1\) variation is
\[
  -(z_1+z_2)G_{CC}(z)+2z_2G_{CC}(z)
  =
  -z\,G_{CC}(z).
\]
It must vanish for \(z\ne0\), so \(G_{CC}=0\) and therefore \(A=0\).  The
remaining \(L_0\) equations give the displayed forms with \(b=B\) and
\(d=D_0\).  The \(L_1\) equation for \(G_{DD}\) is then automatic:
the primary differential part gives
\(-2b(z_1+z_2)z^{-2h}\), while the two nilpotent insertions contribute
\(2b(z_1+z_2)z^{-2h}\).

Finally replace \(D\) by \(D+\lambda C\).  Since \(G_{CC}=0\),
\[
  G_{CD}\mapsto G_{CD},\qquad
  G_{DD}\mapsto G_{DD}+2\lambda G_{CD}.
\]
Thus \(b\) is unchanged and \(d\) is shifted to \(d+2\lambda b\).
\end{proof}

The ordinary character sees only the trace of
\(q^{L_0-c/24}\).  On the span of \(C,D\),
\[
  \operatorname{Tr}\bigl(q^{L_0-c/24}\bigr)
  =
  2q^{h-c/24},
\]
because the nilpotent coefficient \(2\pi\ii\tau N\) has trace zero.  Thus an
ordinary character records the generalized eigenvalue and multiplicity but
not the extension class producing the logarithmic partner.  In a finite-length
\(C_2\)-cofinite logarithmic theory, the genus-one space closed under
modular transformations is therefore larger than the span of ordinary
characters: it includes generalized characters and pseudo-trace functions on
projective modules.  A pseudo-trace is the replacement for the usual matrix
trace when the endomorphism algebra of a projective module is not semisimple;
it is a linear functional with the cyclicity property needed for torus
sewing, and it can couple to the nilpotent part invisible to the ordinary
trace.

The semisimple Verlinde formula
\eqref{eq:verlinde-formula} is consequently replaced by categorical
statements about the Grothendieck ring, projective modules, modified traces,
and the factorizable braided finite tensor category attached to the
logarithmic chiral theory.  The right question is not whether the same matrix
formula survives unchanged, but which categorical pairing diagonalizes which
quotient of the tensor product.  This is one of the places where the
algebraic formulation is essential: it names the objects whose semisimplicity
has failed and identifies the extra data needed to recover modular sewing.

\section{Output}

The output of this chapter is a hierarchy of definitions and hypotheses:
The construction has the following order.  A VOA defines modules and chiral
blocks.  Chiral blocks sew over the moduli space of pointed curves.  In the
semisimple finite case, sewing data assemble into a modular tensor category.
The full CFT is then obtained by choosing a compatible pairing of left and
right chiral theories, equivalently by imposing the modular-invariance and
factorization constraints on torus and higher-genus amplitudes.
The Ising model gives the finite rational example.  Logarithmic theories show
which parts of the rational construction depend on semisimplicity and which
parts persist after replacing simple-module linear algebra by finite tensor
category data.
