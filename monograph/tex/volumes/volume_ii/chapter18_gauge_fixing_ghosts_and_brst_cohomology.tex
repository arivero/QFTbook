\chapter{Gauge Fixing, Ghosts, and BRST Cohomology}
\label{ch:volume-ii-18}

\section{Gauge Orbits and Local Coordinates}

Let \(M\) be the spacetime manifold and let \(G\) be the compact gauge group
whose Lie algebra conventions were fixed in
Chapter~\ref{ch:vol2-classical-yang-mills-matter}.  On a fixed local
trivialization, the space of gauge potentials is the affine space
\[
  \mathcal A=\Omega^1(M,\mathfrak g).
\]
Here \(\mathfrak g=i\,\mathfrak g_{\mathrm{ah}}\) is the Hermitian coordinate
space associated with the anti-Hermitian Lie algebra
\(\mathfrak g_{\mathrm{ah}}=\operatorname{Lie}(G)\) in a unitary
representation.  If \(X,Y\in\mathfrak g\), the compact real Lie bracket is
\[
  [X,Y]_{\mathrm H}:=-\ii[X,Y],
\]
where the bracket on the right is the matrix commutator in a unitary
representation.
The gauge transformations form the infinite-dimensional group
\[
  \mathcal G=C^\infty(M,G).
\]
This group must be distinguished from the finite-dimensional internal group
\(G\).  In local coordinates near the identity, a gauge transformation can be
written as
\[
  g(x)=\exp\bigl(\ii\zeta_a(x)t^a\bigr),
  \qquad
  \xi^\alpha\equiv \zeta_a(x),\qquad
  \alpha=(a,x).
\]
Repeated condensed indices include sums over Lie algebra labels and integrals
over spacetime; for example
\begin{equation}
  f_\alpha g^\alpha
  =
  \sum_a\int_M f_a(x)g^a(x)\,\dd^D x .
  \label{eq:brst-condensed-index-notation}
\end{equation}

The action of \(g\in\mathcal G\) on \(A\in\mathcal A\) is
\[
  A_\mu\longmapsto A_\mu^g
  =
  gA_\mu g^{-1}+\ii g\partial_\mu g^{-1}.
\]
The infinitesimal action associated with
\(\zeta=\zeta^a t^a\in C^\infty(M,\mathfrak g)\) is
\[
  \delta_\zeta A_\mu=D_\mu(A)\zeta,
  \qquad
  D_\mu(A)\zeta=\partial_\mu\zeta+[A_\mu,\zeta]_{\mathrm H},
  \qquad
  (D_\mu(A)\zeta)^a
  =
  \partial_\mu\zeta^a+f^{a}{}_{bc}A_\mu^b\zeta^c .
\]
Let \(\phi\) denote the complete list of fields: gauge potentials and, when
present, matter fields.  At a fixed ultraviolet and infrared regulator, before
the gauge orbits are divided out, the gauge-field integral is an integral over a
regulated field space \(\mathcal F_\Lambda\),
\begin{equation}
  Z_{\mathrm{unfixed},\Lambda}
  =
  \int_{\mathcal F_\Lambda}\dd\mu_\Lambda(\phi)\,
  \exp(\ii S_\Lambda[\phi]),
  \qquad
  S[\phi^\xi]=S[\phi],
  \qquad
  \dd\mu_\Lambda(\phi^\xi)=\dd\mu_\Lambda(\phi).
  \label{eq:unfixed-gauge-path-integral}
\end{equation}
Here \(\dd\mu_\Lambda\) is the regulator measure induced by the chosen
regularization of the bosonic and fermionic variables.  Equation
\eqref{eq:unfixed-gauge-path-integral} is not a definition of a gauge theory by
itself: if the gauge group acts freely, it integrates separately over orbit
directions and over directions transverse to the orbits; if stabilizers or
Gribov phenomena are present, the same statement holds only in sufficiently
small coordinate charts.  In canonical language the orbit directions are the
directions generated by first-class constraints.  Covariant perturbation theory
therefore chooses local coordinates transverse to the orbits and keeps track of
the corresponding Jacobian.

A gauge condition is a collection of functionals \(F^A[\phi]\), with the index
\(A\) carrying the same number of local degrees of freedom as the gauge
parameter index \(\alpha\).  The intended local slice is \(F^A[\phi]=0\).
Figure~\ref{fig:fp-gauge-slice} records the geometric content.

\begin{figure}[H]
  \centering
  \resizebox{0.86\textwidth}{!}{%
  \begin{tikzpicture}[>=Stealth,x=1cm,y=1cm,every node/.style={font=\scriptsize}]
    \foreach \x/\bend in {-3.6/0.25,-2.1/-0.35,-0.55/0.28,1.05/-0.22,2.65/0.35} {
      \draw[blue!70!black,line width=0.95pt]
        (\x,-1.8)
        .. controls ({\x+\bend},-0.85) and ({\x-\bend},0.75) ..
        ({\x+0.08},1.85);
    }
    \draw[red!75!black,line width=1.15pt]
      (-4.35,-0.55)
      .. controls (-2.55,0.45) and (-1.15,0.45) ..
      (-0.15,-0.05)
      .. controls (1.0,-0.65) and (2.45,-0.25) ..
      (4.25,0.65);

    \node[blue!70!black,fill=white,inner sep=2pt] at (-3.0,2.12)
      {gauge orbits};
    \node[red!75!black,fill=white,inner sep=2pt] at (2.65,1.08)
      {gauge slice \(F^A[\phi]=0\)};

    \fill[black] (-0.95,0.42) circle (2.4pt);
    \node[above left] at (-0.95,0.42) {\(\phi\)};
    \fill[purple!80!black] (-0.42,-0.08) circle (2.4pt);
    \node[below right,purple!80!black] at (-0.42,-0.08) {\(\phi^\xi\)};
    \draw[-{Latex[length=2mm]},purple!80!black,line width=0.9pt]
      (-0.88,0.27) .. controls (-0.55,0.08) and (-0.36,0.03) .. (-0.42,-0.03);
    \node[purple!80!black,fill=white,inner sep=2pt] at (-1.35,-0.55)
      {gauge transformation};

  \end{tikzpicture}
  }
  \caption{A local gauge condition is a slice through field space.  The blue
  curves are gauge orbits, the red curve is the surface \(F^A[\phi]=0\), and
  \(\phi\) and \(\phi^\xi\) are two representatives of the same gauge orbit.
  The Faddeev--Popov determinant is the Jacobian that converts integration
  along the orbit to integration through the slice.}
  \label{fig:fp-gauge-slice}
\end{figure}

The infinitesimal variation of the gauge condition along the orbit defines the
Faddeev--Popov matrix
\begin{equation}
  \mathcal M^A{}_\alpha[\phi]
  :=
  \left.
  \frac{\partial F^A[\phi^\xi]}{\partial \xi^\alpha}
  \right|_{\xi=\id}.
  \label{eq:fp-condensed-operator-definition}
\end{equation}
For Yang--Mills gauge potentials alone this is the distributional operator
\begin{equation}
  \mathcal M^{ab}_F(A;x,y)
  =
  \left.
  \frac{\delta F^a(A^{\exp(\ii\zeta)})(x)}
       {\delta\zeta^b(y)}
  \right|_{\zeta=0}.
  \label{eq:fp-operator-definition}
\end{equation}
In a field-space region where the slice is transverse to the orbit, and after
separating zero modes associated with unfixed global transformations, the local
Faddeev--Popov identity is
\begin{equation}
  1
  =
  \int_{\mathcal G}D\xi\,
  \delta\bigl(F^A[\phi^\xi]\bigr)
  \det\left(
    \frac{\partial F^A[\phi^\xi]}{\partial\xi^\alpha}
  \right).
  \label{eq:fp-local-identity}
\end{equation}
Inserting \eqref{eq:fp-local-identity} in
\eqref{eq:unfixed-gauge-path-integral} and changing variables
\(\phi\mapsto \phi^{\xi^{-1}}\) gives a factor depending only on integration
over the gauge group.  More explicitly, the determinant at a general point
\(\xi\) on the orbit splits as
\[
  \left.
  \det
  \frac{\partial F^A[\phi^{\xi'}]}{\partial\xi'^\alpha}
  \right|_{\xi'=\xi}
  =
  \left.
  \det
  \frac{\partial(\xi''\circ\xi)^\alpha}{\partial\xi''^\beta}
  \right|_{\xi''=\id}
  \left.
  \det
  \frac{\partial F^A[\phi^{\xi''}]}{\partial\xi''^\alpha}
  \right|_{\xi''=\id}.
\]
The first factor converts the coordinate measure \(D\xi\) to the right Haar
measure \([D\xi]_{\mathrm H}\), characterized formally by
\begin{equation}
  [D(\xi\circ\xi_1)]_{\mathrm H}=[D\xi]_{\mathrm H},
  \qquad
  \xi_1\in\mathcal G \ \hbox{fixed}.
  \label{eq:functional-haar-measure-property}
\end{equation}
Thus
\[
  \int_{\mathcal G}[D\xi]_{\mathrm H}
\]
is the formal volume of the gauge-transformation group, and the normalized
gauge-theory path integral is
\begin{equation}
  Z
  =
  \frac{1}{\int_{\mathcal G}[D\xi]_{\mathrm H}}
  \int[D\phi]\,\exp(\ii S[\phi]).
  \label{eq:gauge-theory-divide-by-gauge-volume}
\end{equation}
The same expression, written in local gauge-fixed coordinates, is
\begin{equation}
  Z
  =
  \int[D\phi]\,
  \delta\bigl(F^A[\phi]\bigr)
  \Delta_{\mathrm{FP}}[\phi]\,
  \exp(\ii S[\phi]),
  \qquad
  \Delta_{\mathrm{FP}}[\phi]=\det\mathcal M[\phi].
  \label{eq:fp-gauge-fixed-integral}
\end{equation}
Global obstructions to a single smooth slice are genuine features of
nonabelian gauge theory.  The construction in this chapter is the local
perturbative construction around a chosen region of field space.

The determinant in \eqref{eq:fp-gauge-fixed-integral} is represented by
Grassmann-odd variables \(b_A\) and \(c^\alpha\):
\begin{equation}
  \Delta_{\mathrm{FP}}[\phi]
  =
  \int[Db][Dc]\,
  \exp\left[
    \ii b_A
    \left.
    \frac{\partial F^A[\phi^\xi]}{\partial\xi^\alpha}
    \right|_{\xi=\id}
    c^\alpha
  \right].
  \label{eq:general-ghost-determinant-bc}
\end{equation}
The field \(c^\alpha\) is the ghost associated with the gauge parameter
\(\xi^\alpha\), and \(b_A\) is the antighost associated with the gauge condition
\(F^A\).  The delta functional is represented by a commuting auxiliary field
\(B_A\):
\begin{equation}
  \delta\bigl(F^A[\phi]\bigr)
  =
  \int[DB]\,
  \exp\bigl(\ii B_A F^A[\phi]\bigr).
  \label{eq:delta-functional-b-field}
\end{equation}
The gauge-fixed action in these condensed variables is therefore
\begin{equation}
  S_{\mathrm{tot}}[\phi,B,b,c]
  =
  S[\phi]+S_{\mathrm{GF}}[\phi,B,b,c],
  \qquad
  S_{\mathrm{GF}}
  =
  B_AF^A[\phi]+b_A\mathcal M^A{}_\alpha[\phi]c^\alpha .
  \label{eq:general-gauge-fixed-action}
\end{equation}
The term \(S_{\mathrm{GF}}\) is not gauge invariant; its controlling symmetry is
the nilpotent BRST symmetry constructed below.

\section{Covariant Gauges and Ghost Fields}

The covariant linear gauge condition is
\[
  F^a(A)=\partial^\mu A_\mu^a .
\]
For this choice,
\begin{equation}
  \mathcal M^{ab}(A)
  =
  \partial^\mu D_\mu^{ab}(A),
  \qquad
  (D_\mu c)^a
  =
  \partial_\mu c^a+f^{a}{}_{bc}A_\mu^b c^c .
  \label{eq:covariant-fp-operator}
\end{equation}
For this local gauge condition the condensed labels are
\[
  A=(a,x),\qquad \alpha=(b,y),
\]
and \(\mathcal M^{ab}(A;x,y)=\partial^\mu D_\mu^{ab}(A)\delta^{(D)}(x-y)\).
The \(b_A\) notation used in
\eqref{eq:general-ghost-determinant-bc} is related to the conventional
antighost by
\[
  \bar c_A=-b_A .
\]
With this convention the ghost term in the Lagrangian has the sign displayed
below, while the BRST transformation of the old variable is \(s b_A=-B_A\).
The determinant of \(\mathcal M(A)\) is represented by Grassmann-valued scalar
fields \(c^a(x)\) and \(\bar c^a(x)\) in the adjoint representation:
\begin{equation}
  \det\mathcal M(A)
  =
  \int [D\bar c\,Dc]\,
  \exp\left\{-\ii\int_M \bar c^a\mathcal M^{ab}(A)c^b\,\dd^D x\right\}.
  \label{eq:ghost-determinant}
\end{equation}
This equality is understood up to a field-independent normalization and phase:
in a finite-dimensional Grassmann Gaussian, changing the sign multiplying
\(\bar c\,\mathcal M c\) multiplies the integral by a power of \(-1\) and
\(\ii\), but does not change its dependence on \(A\).  The sign in
\eqref{eq:ghost-determinant} is chosen so that the ghost term agrees with the
BRST gauge-fixing fermion below in the Minkowski weight \(\exp(\ii S)\).
The field \(c\) is called the ghost and \(\bar c\) the antighost.  They are
Lorentz scalars with fermionic statistics.  Their role is determined by
\eqref{eq:ghost-determinant}: they encode the Jacobian of gauge fixing and
produce the loop contributions required by gauge covariance of the
renormalized theory.

It is useful to introduce an auxiliary real field \(B^a(x)\), the
Nakanishi--Lautrup field.  For a real parameter \(\xi\), and for the
Yang--Mills normalization
\[
  \mathcal L_{\mathrm{YM}}
  =
  -\frac1{4g_{\mathrm{YM}}^2}F^a_{\mu\nu}F^{a\mu\nu},
\]
the gauge-fixing and ghost Lagrangian density can be written
\begin{equation}
  \mathcal L_{\mathrm{gf+gh}}
  =
  B^a\partial^\mu A_\mu^a
  +\frac{g_{\mathrm{YM}}^2\xi}{2}B^aB^a
  -\bar c^a\,\partial^\mu D_\mu^{ab}(A)c^b .
  \label{eq:gf-ghost-lagrangian}
\end{equation}
Eliminating \(B\) by its algebraic equation of motion gives
\[
  B^a=-\frac1{g_{\mathrm{YM}}^2\xi}\partial^\mu A_\mu^a,
  \qquad
  \mathcal L_{\mathrm{gf}}
  =
  -\frac1{2g_{\mathrm{YM}}^2\xi}
  (\partial^\mu A_\mu^a)(\partial^\nu A_\nu^a),
\]
and Landau gauge is the limiting gauge condition obtained by taking
\(\xi\to0\) after the regulated Gaussian \(B\)-field integral has been
defined.
Setting \(g_{\mathrm{YM}}=1\), or rescaling the gauge potential to canonical
quadratic normalization, gives the more common version without the displayed
factor of \(g_{\mathrm{YM}}^2\) in the \(B^2\) term.

\section{The BRST Differential}

The gauge-fixed field algebra is a graded-commutative algebra generated by
\[
  A_\mu^a,\qquad c^a,\qquad \bar c^a,\qquad B^a,
\]
and by matter fields when present.  Assign ghost numbers
\[
  \operatorname{gh}(A_\mu)=0,\qquad
  \operatorname{gh}(B)=0,\qquad
  \operatorname{gh}(c)=1,\qquad
  \operatorname{gh}(\bar c)=-1 .
\]
The BRST differential \(s\) is an odd derivation of ghost number \(+1\).  On
the Yang--Mills sector it is defined by
\begin{equation}
  sA_\mu^a=(D_\mu c)^a,\qquad
  sc^a=-\frac12 f^{a}{}_{bc}c^b c^c,\qquad
  s\bar c^a=B^a,\qquad
  sB^a=0 .
  \label{eq:brst-transformations}
\end{equation}
In condensed field-space notation this reads
\begin{equation}
  s\phi=c^\alpha S_\alpha\phi,\qquad
  sB_A=0,\qquad
  sb_A=-B_A,\qquad
  sc^\alpha=-\frac12f^\alpha{}_{\beta\gamma}c^\beta c^\gamma,
  \label{eq:brst-condensed-transformations}
\end{equation}
where
\[
  S_\alpha\phi
  =
  \left.
  \frac{\partial\phi^\xi}{\partial\xi^\alpha}
  \right|_{\xi=\id},
  \qquad
  [S_\beta,S_\gamma]=f^\alpha{}_{\beta\gamma}S_\alpha .
\]
The two notations agree because \(b_A=-\bar c_A\).  If \(\epsilon\) is a
constant Grassmann-odd parameter, the finite-parity infinitesimal variation is
\(\delta_B X=\epsilon\,sX\); in particular \(\epsilon c^\alpha\) is even, as a
gauge parameter must be.
The ghost \(c=c^a t^a\) is an odd field valued in the same Hermitian coordinate
space as the infinitesimal gauge parameter.  For an odd \(\mathfrak g\)-valued
field, the expression \([c,c]_{\mathrm H}\) is defined by combining the Lie
bracket with the Grassmann product:
\[
  [c,c]_{\mathrm H}^a=f^a{}_{bc}c^b c^c .
\]
Thus \(sc=-\frac12[c,c]_{\mathrm H}\).  The antighost \(\bar c\) is an
independent odd adjoint-valued scalar of ghost number \(-1\), and \(B\) is an
even adjoint-valued scalar of ghost number \(0\).
For a matter field \(\psi\in V_R\), with representation matrices \(t_R^a\),
\begin{equation}
  s\psi=\ii c^a t_R^a\psi,
  \qquad
  s\bar\psi=-\ii\bar\psi\,c^a t_R^a .
  \label{eq:brst-matter-transformations}
\end{equation}
The derivation rule is graded:
\[
  s(XY)=(sX)Y+(-1)^{|X|}X(sY),
\]
where \(|X|\) is the Grassmann parity of \(X\).

Nilpotency is an algebraic consequence of the Jacobi identity.  For any
original field \(\phi\), the condensed notation gives
\[
\begin{aligned}
  s^2\phi
  &=
  -\frac12 f^\alpha{}_{\beta\gamma}c^\beta c^\gamma S_\alpha\phi
  -c^\alpha c^\beta S_\beta S_\alpha\phi
\\
  &=
  -\frac12 f^\alpha{}_{\beta\gamma}c^\beta c^\gamma S_\alpha\phi
  +\frac12 c^\alpha c^\beta[S_\alpha,S_\beta]\phi
  =
  0 .
\end{aligned}
\]
For the ghost, using the graded derivation rule gives
\[
  s^2c^a
  =
  \frac14 f^a{}_{bc} f^b{}_{de} c^d c^e c^c
  -\frac14 f^a{}_{bc} f^c{}_{de} c^b c^d c^e .
\]
After antisymmetrizing over the three odd ghost factors, this is
\[
  s^2c^a
  =
  -\frac1{12}
  \left(
    f^a{}_{be}f^e{}_{cd}
    +f^a{}_{ce}f^e{}_{db}
    +f^a{}_{de}f^e{}_{bc}
  \right)c^b c^c c^d
  =
  0 .
\]
For the gauge field, the matrix form of the same computation is
\[
  s^2A_\mu
  =
  D_\mu(sc)+[D_\mu c,c]_{\mathrm H}
  =
  -\frac12D_\mu[c,c]_{\mathrm H}+[D_\mu c,c]_{\mathrm H}
  =
  0,
\]
because \(D_\mu[c,c]_{\mathrm H}=2[D_\mu c,c]_{\mathrm H}\) for odd \(c\).
For a matter field, the representation identity
\([t_R^a,t_R^b]=\ii f^{c}{}_{ab}t_R^c\) gives
\[
  s^2\psi
  =
  \ii(sc^a)t_R^a\psi+c^a c^b t_R^a t_R^b\psi
  =
  -\frac{\ii}{2} f^a{}_{bc}c^b c^c t_R^a\psi
  +\frac12 c^a c^b[t_R^a,t_R^b]\psi
  =
  0,
\]
and similarly for \(\bar\psi\).  Hence
\begin{equation}
  s^2=0.
  \label{eq:brst-nilpotency}
\end{equation}
Thus \(s\) is the infinitesimal gauge-orbit differential with the even gauge
parameter replaced by the odd field \(c\), combined with the
Chevalley--Eilenberg differential of the gauge algebra and the nonminimal
contractible pair \((\bar c,B)\).

Let
\[
  \Psi
  =
  \int_M
  \bar c^a\left(
    \partial^\mu A_\mu^a+\frac{g_{\mathrm{YM}}^2\xi}{2}B^a
  \right)\dd^D x .
\]
Using \eqref{eq:brst-transformations},
\begin{equation}
  s\Psi
  =
  \int_M
  \left[
  B^a\partial^\mu A_\mu^a
  +\frac{g_{\mathrm{YM}}^2\xi}{2}B^aB^a
  -\bar c^a\partial^\mu D_\mu^{ab}(A)c^b
  \right]\dd^D x .
  \label{eq:gauge-fixing-fermion}
\end{equation}
For \(\xi=0\), this is the covariant-gauge specialization of the condensed
formula
\[
  S_{\mathrm{GF}}
  =
  -s\bigl(b_AF^A[\phi]\bigr)
  =
  B_AF^A[\phi]+b_A\mathcal M^A{}_\alpha[\phi]c^\alpha .
\]
Equivalently, if the BRST charge \(Q_B\) is defined by the convention
\begin{equation}
  \delta_B X=\ii\epsilon\,Q_B\cdot X,
  \qquad
  Q_B=-\ii s,
  \label{eq:qb-convention-from-s}
\end{equation}
then
\begin{equation}
  S_{\mathrm{GF}}
  =
  -\ii Q_B\cdot\bigl(b_AF^A[\phi]\bigr),
  \qquad
  Q_B^2=0 .
  \label{eq:b-notation-gauge-fixing-qb-exact}
\end{equation}
The \(\xi B^2\) term in \eqref{eq:gauge-fixing-fermion} is an additional
BRST-exact deformation of the same gauge-fixing fermion and does not alter the
cohomology.
The full gauge-fixed action is therefore
\[
  S_{\mathrm{gf}}
  =
  S_{\mathrm{YM}}+S_{\mathrm{matter}}+s\Psi .
\]
Since \(S_{\mathrm{YM}}+S_{\mathrm{matter}}\) is gauge invariant and \(s^2=0\),
one obtains \(sS_{\mathrm{gf}}=0\).  The gauge-fixed action is therefore
controlled by a nilpotent fermionic symmetry acting on the enlarged field
space, including ghosts, antighosts, and the Nakanishi--Lautrup field.

\section{Local BRST Cohomology}

The BRST differential acts on local expressions before any functional integral
is evaluated.  Let \(\mathcal F_{\mathrm{loc}}\) denote the graded-commutative
algebra of local functions: polynomials or formal power series in the fields
\[
  A_\mu,\quad c,\quad \bar c,\quad B,\quad \psi,\quad \bar\psi
\]
and finitely many of their spacetime derivatives, with coefficients smooth in
the spacetime point when explicit \(x\)-dependence is allowed.  The differential
\(s\) is extended to derivatives by
\[
  s(\partial_{\nu_1}\cdots\partial_{\nu_r}\Phi)
  =
  \partial_{\nu_1}\cdots\partial_{\nu_r}(s\Phi)
\]
for every generator \(\Phi\).  The local BRST cohomology at ghost number \(g\)
is
\[
  H^g(s;\mathcal F_{\mathrm{loc}})
  =
  \frac{\{a\in\mathcal F_{\mathrm{loc}}:\operatorname{gh}(a)=g,\ sa=0\}}
       {\{a=sb:\operatorname{gh}(b)=g-1\}} .
\]
At ghost number zero, this cohomology is the local algebraic form of
gauge-invariant composite operators, with BRST-exact representatives removed.

Integrated local functionals require a second quotient because Lagrangian
densities differing by total derivatives define the same action on manifolds
without boundary, or the same bulk functional after boundary data have been
specified.  Let \(\Omega^p_{\mathrm{loc}}\) be the space of local \(p\)-forms and
let
\[
  d=dx^\mu\partial_\mu
\]
be the spacetime exterior derivative.  With the convention that \(s\) and \(d\)
are odd derivations on local forms, they anticommute:
\[
  sd+ds=0 .
\]
For a \(D\)-dimensional spacetime, an integrated local functional is represented
by a local \(D\)-form \(a\).  The cohomology relevant for local functionals is
the relative cohomology \(H^{g,D}(s\mid d)\).  Its cocycles and coboundaries are
\[
  sa+dm=0,
  \qquad
  a\sim a+sb+dn ,
\]
where
\[
  a\in\Omega^D_{\mathrm{loc}},\quad
  m,n\in\Omega^{D-1}_{\mathrm{loc}},\quad
  b\in\Omega^D_{\mathrm{loc}},
\]
with ghost numbers chosen so that all displayed terms have ghost number \(g+1\)
in the first equation and ghost number \(g\) in the equivalence relation.  Thus
\(H^{0,D}(s\mid d)\) classifies BRST-invariant local counterterm densities
modulo field redefinitions and total derivatives, while \(H^{1,D}(s\mid d)\)
is the algebraic home of possible local gauge anomalies.  The anomaly chapters
will compute the relevant representatives in four-dimensional chiral gauge
theories; the present chapter fixes the cohomological language.

The pair \((\bar c,B)\) belongs to the nonminimal gauge-fixing sector.  Since
\[
  s\bar c^a=B^a,\qquad sB^a=0,
\]
and the BRST transformations of the minimal variables \(A,c,\psi,\bar\psi\) do
not depend on \(\bar c\) or \(B\), this pair is contractible in local BRST
cohomology.  The doublet lemma below gives the explicit homotopy.  Consequently,
the nonminimal variables are essential for writing local propagators and
Slavnov--Taylor identities, but the cohomology classes of gauge-invariant local
operators, counterterms, and anomalies may be represented in the minimal sector
under the standard regularity assumptions used in perturbative Yang--Mills
theory.

\section{Cohomology of States and Observables}

In canonical quantization of the gauge-fixed theory, the state space before
the BRST quotient is an indefinite inner-product space \(\mathcal K\).  At a
fixed time a state can be represented, in a Schrödinger notation, by a
wavefunctional
\[
  \Psi[\phi,B,b,c]\big|_{x^0=t}.
\]
The first-order functional differential operator \(Q_B\) in
\eqref{eq:qb-convention-from-s} is promoted to an operator
\(\widehat Q_B\) on \(\mathcal K\):
\[
  \widehat Q_B\ket{\Psi}
  =
  \ket{Q_B\cdot\Psi},
  \qquad
  \widehat Q_B^{\,2}=0 .
\]
Insertion of a functional \(R[\Phi]\), where \(\Phi=(\phi,B,b,c)\), corresponds
to an operator \(\widehat R\), and the insertion \(Q_B\cdot R\) corresponds to
the graded commutator with \(\widehat Q_B\), with the sign fixed by the parity
of \(R\).  In the rest of this section we write \(Q\) for \(\widehat Q_B\).

A state is admissible when
\begin{equation}
  Q\ket{\Psi}=0 .
  \label{eq:brst-admissible-state}
\end{equation}
This condition is selected by gauge-fixing independence.  Consider the
transition amplitude between two admissible boundary states,
\[
  \mathcal A_F(\Psi_f,\Psi_i)
  =
  \int[D\Phi]_{t_i}^{t_f}\,
  \Psi_f^*[\Phi(t_f)]\,
  \exp(\ii S_F[\Phi])\,
  \Psi_i[\Phi(t_i)],
\]
where the subscript \(F\) records the chosen gauge condition.  If
\(F^A[\phi]\) is deformed to \(F^A[\phi]+\overline\delta F^A[\phi]\), then
\eqref{eq:b-notation-gauge-fixing-qb-exact} gives
\[
  \overline\delta S_F[\Phi]
  =
  -\ii Q_B\cdot\bigl(b_A\overline\delta F^A[\phi]\bigr).
\]
Therefore, to first order in the deformation,
\begin{align}
  \overline\delta\,\mathcal A_F(\Psi_f,\Psi_i)
  &=
  \int[D\Phi]\,
  \Psi_f^*\,\exp(\ii S_F)\,\Psi_i\,
  Q_B\cdot\bigl(b_A\overline\delta F^A\bigr)
  \notag\\
  &=
  \int[D\Phi]\,
  Q_B\cdot
  \left(
    \Psi_f^*\,\exp(\ii S_F)\,\Psi_i\,
    b_A\overline\delta F^A
  \right)
  =
  0 .
  \label{eq:brst-gauge-fixing-independence}
\end{align}
The last equality assumes that the functional measure is BRST invariant and
that the BRST integration by parts has no boundary contribution in field
space.  The equalities also use \(Q\ket{\Psi_i}=0\), the corresponding
condition on the final bra, and \(Q_B\cdot S_F=0\).

Nilpotency implies that \(Q\ket{\chi}\) is admissible for every
\(\ket{\chi}\).  Such a state has zero matrix element against any admissible
state when \(Q\) is symmetric with respect to the indefinite inner product:
\[
  \bra{\Psi}Q\ket{\chi}=0,
  \qquad
  Q\ket{\Psi}=0 .
\]
Thus
\[
  \ket{\Psi}\sim \ket{\Psi}+Q\ket{\chi}
\]
is the equivalence relation on admissible states.  The physical state space is
the ghost-number-zero BRST cohomology
\begin{equation}
  \mathcal H_{\mathrm{phys}}
  =
  H^0(Q,\mathcal K)
  =
  \frac{\ker(Q:\mathcal K^0\to\mathcal K^1)}
       {\operatorname{im}(Q:\mathcal K^{-1}\to\mathcal K^0)} .
  \label{eq:brst-physical-state-cohomology}
\end{equation}
Here \(\mathcal K^n\) is the subspace of ghost number \(n\).  Gauge-invariant
local operators define classes in the ghost-number-zero cohomology of \(s\),
while BRST-exact insertions have vanishing matrix elements between physical
states when the BRST symmetry is preserved by the regulator and
renormalization.

The perturbative \(S\)-matrix of a gauge-fixed nonabelian theory is an operator
first constructed on \(\mathcal K\) and then descended to
\(\mathcal H_{\mathrm{phys}}\).  Asymptotic states and LSZ define scattering;
gauge fixing supplies local Green-function rules whose matrix elements are
well defined on BRST cohomology classes.

\section{Positivity of the BRST Quotient}

The quotient in \eqref{eq:brst-physical-state-cohomology} is a Hilbert-space
construction only after positivity has been checked.  Assume the indefinite
inner product on \(\mathcal K\) is nondegenerate and that \(Q\) is symmetric
with respect to it:
\[
  \langle Q\chi,\psi\rangle=\langle\chi,Q\psi\rangle .
\]
If \(\psi\in\ker Q\) and \(\eta=Q\chi\), then
\[
  \langle\eta,\psi\rangle
  =
  \langle Q\chi,\psi\rangle
  =
  \langle\chi,Q\psi\rangle
  =
  0.
\]
Thus exact states are orthogonal to all closed states, including themselves.
The inner product therefore descends to the quotient
\(\ker Q/\operatorname{im}Q\).  The remaining requirement is that the induced
form on ghost number zero cohomology be positive and that zero-norm closed
states represent the zero cohomology class.  When this holds, completing the
quotient gives the physical Hilbert space.

\begin{theorem}[Kugo--Ojima positivity criterion in a BRST doublet sector]
Let \(Q^2=0\) be symmetric for the nondegenerate indefinite inner product on
the ghost-graded space \(\mathcal K\).  Assume that the ghost-number-zero closed
subspace admits a direct decomposition
\[
  \ker(Q:\mathcal K^0\to\mathcal K^1)
  =
  \mathcal P\oplus \operatorname{im}(Q:\mathcal K^{-1}\to\mathcal K^0),
\]
where the restriction of the inner product to \(\mathcal P\) is positive
definite.  Assume further that every closed state with zero norm lies in
\(\operatorname{im}Q\).  Then
\[
  H^0(Q,\mathcal K)
  =
  \frac{\ker(Q:\mathcal K^0\to\mathcal K^1)}
       {\operatorname{im}(Q:\mathcal K^{-1}\to\mathcal K^0)}
\]
inherits a positive-definite inner product and is isometric to the positive
subspace \(\mathcal P\) by the map that sends a closed state to its
\(\mathcal P\)-component.
\end{theorem}

\begin{proof}
Exact states are orthogonal to closed states by the calculation above, so the
inner product is independent of the representative of a cohomology class.  The
displayed decomposition says that each class has a unique representative in
\(\mathcal P\).  The induced norm of the class is therefore the norm of this
\(\mathcal P\)-representative.  Positivity on \(\mathcal P\) gives a
nonnegative norm, and the additional zero-norm assumption says that a class of
zero norm has an exact representative, hence is the zero class.
\end{proof}

Perturbatively, the unphysical polarizations, ghosts, antighosts, and
Nakanishi--Lautrup modes organize into \(Q\)-doublets.  A doublet has the form
\[
  Q u=v,\qquad Qv=0 .
\]
Any state obtained by acting with such paired excitations is either exact or
paired with an exact state and does not contribute to cohomology.  The
cohomology is then represented by transverse physical polarizations and
gauge-invariant matter states.  This is the cohomological version of the
statement that the physical content of the gauge-fixed construction is carried
by \(Q\)-cohomology classes.

\section{The Doublet Lemma}

The decoupling of unphysical variables has a simple algebraic form.  Suppose a
graded commutative differential algebra contains pairs
\[
  u^i,\qquad v^i,
\]
with
\[
  su^i=v^i,\qquad sv^i=0.
\]
Let \(N\) count the total number of \(u\)'s and \(v\)'s in a polynomial, and
define a homotopy operator \(h\) by
\[
  h v^i=u^i,\qquad h u^i=0,
\]
extended as a graded derivation with the appropriate signs.  On the subalgebra
generated by the doublets one has
\[
  sh+hs=N .
\]
If \(X\) is \(s\)-closed and homogeneous with \(NX=rX\), \(r>0\), then
\[
  X=\frac1r(sh+hs)X=s\left(\frac1r hX\right).
\]
Thus every \(s\)-closed expression with positive doublet number is
\(s\)-exact.  The cohomology is represented by expressions independent of the
doublet variables.

In perturbative gauge theory the longitudinal gauge modes, antighosts,
Nakanishi--Lautrup variables, and their paired ghost excitations are organized
in precisely this way after linearization around the perturbative vacuum.  The
lemma explains why physical cohomology depends on gauge-invariant transverse
and matter data, while the doublet variables remain necessary in intermediate
local formulas for propagators, counterterms, and Slavnov--Taylor identities.

\section{Slavnov--Taylor Identities and Counterterms}

BRST symmetry gives functional identities for the generating functional and
for the 1PI effective action.  In a compact notation, introduce sources
\(K^{a\mu}\) and \(L^a\) coupled to \(sA_\mu^a\) and \(sc^a\).  The 1PI
effective action \(\Gamma[A,c,\bar c,B;K,L]\) obeys the Slavnov--Taylor
identity
\begin{equation}
  \mathcal S(\Gamma)
  :=
  \int_M
  \left[
  \frac{\delta\Gamma}{\delta K^{a\mu}}
  \frac{\delta\Gamma}{\delta A_\mu^a}
  +
  \frac{\delta\Gamma}{\delta L^a}
  \frac{\delta\Gamma}{\delta c^a}
  +
  B^a\frac{\delta\Gamma}{\delta\bar c^a}
  \right]\dd^D x
  =
  0 .
  \label{eq:slavnov-taylor-identity}
\end{equation}
The sources \(K^{a\mu}\) and \(L^a\) have ghost numbers \(-1\) and \(-2\),
respectively, so that the source terms \(K^{a\mu}sA^a_\mu\) and \(L^a sc^a\)
have ghost number zero.  They are introduced because the BRST transformations
of \(A_\mu\) and \(c\) are nonlinear composite fields.  The
Batalin--Vilkovisky formulation replaces this case-by-case source assignment:
for every field \(\Phi^A\), one introduces an antifield \(\Phi^*_A\) of
opposite Grassmann parity and ghost number
\[
  \operatorname{gh}(\Phi^*_A)=-1-\operatorname{gh}(\Phi^A),
\]
and encodes gauge symmetry by an odd Poisson bracket, the antibracket, and a
master equation.  The present Slavnov--Taylor identity is the gauge-fixed
Yang--Mills specialization of that homological structure.
Chapter~\ref{ch:bv-master-formalism} develops the BV formalism as the natural
framework for gauge-theory
1PI effective actions, Wilsonian effective actions, and renormalization
compatible with gauge symmetry.
The displayed formula suppresses the sign refinements required by left and
right functional derivatives for odd variables; its content is that the
quantum effective action is invariant under the BRST differential after the
nonlinear variations have been sourced.

Renormalization is compatible with gauge theory when counterterms can be
chosen so that \eqref{eq:slavnov-taylor-identity} remains valid order by
order.  Algebraically, the possible invariant counterterms are classified by
ghost-number-zero local BRST cohomology.  A gauge anomaly is an obstruction at
ghost number \(+1\): it is a local functional \(\mathcal A\) satisfying the
linearized consistency condition
\[
  \mathcal B_\Gamma\mathcal A=0
\]
that cannot be written as \(\mathcal A=\mathcal B_\Gamma X\) for a local
functional \(X\).  The axial anomaly and the nonabelian gauge-anomaly
cancellation conditions are developed in the anomaly chapters below.
